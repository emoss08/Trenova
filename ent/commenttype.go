// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/commenttype"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/google/uuid"
)

// CommentType is the model entity for the CommentType schema.
type CommentType struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BusinessUnitID holds the value of the "business_unit_id" field.
	BusinessUnitID uuid.UUID `json:"businessUnitId"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organizationId"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updatedAt"`
	// Version holds the value of the "version" field.
	Version int `json:"version" validate:"omitempty"`
	// Status holds the value of the "status" field.
	Status commenttype.Status `json:"status" validate:"required,oneof=A I"`
	// Name holds the value of the "name" field.
	Name string `json:"name" validate:"required,max=10"`
	// Severity holds the value of the "severity" field.
	Severity commenttype.Severity `json:"severity" validate:"required,oneof=High Medium Low"`
	// Description holds the value of the "description" field.
	Description string `json:"description" validate:"omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CommentTypeQuery when eager-loading is set.
	Edges        CommentTypeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CommentTypeEdges holds the relations/edges for other nodes in the graph.
type CommentTypeEdges struct {
	// BusinessUnit holds the value of the business_unit edge.
	BusinessUnit *BusinessUnit `json:"business_unit,omitempty"`
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// ShipmentComments holds the value of the shipment_comments edge.
	ShipmentComments []*ShipmentComment `json:"shipmentComments"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes           [3]bool
	namedShipmentComments map[string][]*ShipmentComment
}

// BusinessUnitOrErr returns the BusinessUnit value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CommentTypeEdges) BusinessUnitOrErr() (*BusinessUnit, error) {
	if e.BusinessUnit != nil {
		return e.BusinessUnit, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: businessunit.Label}
	}
	return nil, &NotLoadedError{edge: "business_unit"}
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CommentTypeEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// ShipmentCommentsOrErr returns the ShipmentComments value or an error if the edge
// was not loaded in eager-loading.
func (e CommentTypeEdges) ShipmentCommentsOrErr() ([]*ShipmentComment, error) {
	if e.loadedTypes[2] {
		return e.ShipmentComments, nil
	}
	return nil, &NotLoadedError{edge: "shipment_comments"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CommentType) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case commenttype.FieldVersion:
			values[i] = new(sql.NullInt64)
		case commenttype.FieldStatus, commenttype.FieldName, commenttype.FieldSeverity, commenttype.FieldDescription:
			values[i] = new(sql.NullString)
		case commenttype.FieldCreatedAt, commenttype.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case commenttype.FieldID, commenttype.FieldBusinessUnitID, commenttype.FieldOrganizationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CommentType fields.
func (ct *CommentType) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case commenttype.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ct.ID = *value
			}
		case commenttype.FieldBusinessUnitID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field business_unit_id", values[i])
			} else if value != nil {
				ct.BusinessUnitID = *value
			}
		case commenttype.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				ct.OrganizationID = *value
			}
		case commenttype.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ct.CreatedAt = value.Time
			}
		case commenttype.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ct.UpdatedAt = value.Time
			}
		case commenttype.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				ct.Version = int(value.Int64)
			}
		case commenttype.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				ct.Status = commenttype.Status(value.String)
			}
		case commenttype.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ct.Name = value.String
			}
		case commenttype.FieldSeverity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field severity", values[i])
			} else if value.Valid {
				ct.Severity = commenttype.Severity(value.String)
			}
		case commenttype.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				ct.Description = value.String
			}
		default:
			ct.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CommentType.
// This includes values selected through modifiers, order, etc.
func (ct *CommentType) Value(name string) (ent.Value, error) {
	return ct.selectValues.Get(name)
}

// QueryBusinessUnit queries the "business_unit" edge of the CommentType entity.
func (ct *CommentType) QueryBusinessUnit() *BusinessUnitQuery {
	return NewCommentTypeClient(ct.config).QueryBusinessUnit(ct)
}

// QueryOrganization queries the "organization" edge of the CommentType entity.
func (ct *CommentType) QueryOrganization() *OrganizationQuery {
	return NewCommentTypeClient(ct.config).QueryOrganization(ct)
}

// QueryShipmentComments queries the "shipment_comments" edge of the CommentType entity.
func (ct *CommentType) QueryShipmentComments() *ShipmentCommentQuery {
	return NewCommentTypeClient(ct.config).QueryShipmentComments(ct)
}

// Update returns a builder for updating this CommentType.
// Note that you need to call CommentType.Unwrap() before calling this method if this CommentType
// was returned from a transaction, and the transaction was committed or rolled back.
func (ct *CommentType) Update() *CommentTypeUpdateOne {
	return NewCommentTypeClient(ct.config).UpdateOne(ct)
}

// Unwrap unwraps the CommentType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ct *CommentType) Unwrap() *CommentType {
	_tx, ok := ct.config.driver.(*txDriver)
	if !ok {
		panic("ent: CommentType is not a transactional entity")
	}
	ct.config.driver = _tx.drv
	return ct
}

// String implements the fmt.Stringer.
func (ct *CommentType) String() string {
	var builder strings.Builder
	builder.WriteString("CommentType(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ct.ID))
	builder.WriteString("business_unit_id=")
	builder.WriteString(fmt.Sprintf("%v", ct.BusinessUnitID))
	builder.WriteString(", ")
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", ct.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ct.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ct.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", ct.Version))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", ct.Status))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ct.Name)
	builder.WriteString(", ")
	builder.WriteString("severity=")
	builder.WriteString(fmt.Sprintf("%v", ct.Severity))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(ct.Description)
	builder.WriteByte(')')
	return builder.String()
}

// NamedShipmentComments returns the ShipmentComments named value or an error if the edge was not
// loaded in eager-loading with this name.
func (ct *CommentType) NamedShipmentComments(name string) ([]*ShipmentComment, error) {
	if ct.Edges.namedShipmentComments == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := ct.Edges.namedShipmentComments[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (ct *CommentType) appendNamedShipmentComments(name string, edges ...*ShipmentComment) {
	if ct.Edges.namedShipmentComments == nil {
		ct.Edges.namedShipmentComments = make(map[string][]*ShipmentComment)
	}
	if len(edges) == 0 {
		ct.Edges.namedShipmentComments[name] = []*ShipmentComment{}
	} else {
		ct.Edges.namedShipmentComments[name] = append(ct.Edges.namedShipmentComments[name], edges...)
	}
}

// CommentTypes is a parsable slice of CommentType.
type CommentTypes []*CommentType
