// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/documentclassification"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/emoss08/trenova/ent/predicate"
	"github.com/emoss08/trenova/ent/shipment"
	"github.com/emoss08/trenova/ent/shipmentdocumentation"
	"github.com/google/uuid"
)

// ShipmentDocumentationQuery is the builder for querying ShipmentDocumentation entities.
type ShipmentDocumentationQuery struct {
	config
	ctx                        *QueryContext
	order                      []shipmentdocumentation.OrderOption
	inters                     []Interceptor
	predicates                 []predicate.ShipmentDocumentation
	withBusinessUnit           *BusinessUnitQuery
	withOrganization           *OrganizationQuery
	withShipment               *ShipmentQuery
	withDocumentClassification *DocumentClassificationQuery
	modifiers                  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShipmentDocumentationQuery builder.
func (sdq *ShipmentDocumentationQuery) Where(ps ...predicate.ShipmentDocumentation) *ShipmentDocumentationQuery {
	sdq.predicates = append(sdq.predicates, ps...)
	return sdq
}

// Limit the number of records to be returned by this query.
func (sdq *ShipmentDocumentationQuery) Limit(limit int) *ShipmentDocumentationQuery {
	sdq.ctx.Limit = &limit
	return sdq
}

// Offset to start from.
func (sdq *ShipmentDocumentationQuery) Offset(offset int) *ShipmentDocumentationQuery {
	sdq.ctx.Offset = &offset
	return sdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sdq *ShipmentDocumentationQuery) Unique(unique bool) *ShipmentDocumentationQuery {
	sdq.ctx.Unique = &unique
	return sdq
}

// Order specifies how the records should be ordered.
func (sdq *ShipmentDocumentationQuery) Order(o ...shipmentdocumentation.OrderOption) *ShipmentDocumentationQuery {
	sdq.order = append(sdq.order, o...)
	return sdq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (sdq *ShipmentDocumentationQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: sdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentdocumentation.Table, shipmentdocumentation.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmentdocumentation.BusinessUnitTable, shipmentdocumentation.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(sdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (sdq *ShipmentDocumentationQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: sdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentdocumentation.Table, shipmentdocumentation.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmentdocumentation.OrganizationTable, shipmentdocumentation.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(sdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShipment chains the current query on the "shipment" edge.
func (sdq *ShipmentDocumentationQuery) QueryShipment() *ShipmentQuery {
	query := (&ShipmentClient{config: sdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentdocumentation.Table, shipmentdocumentation.FieldID, selector),
			sqlgraph.To(shipment.Table, shipment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shipmentdocumentation.ShipmentTable, shipmentdocumentation.ShipmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(sdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDocumentClassification chains the current query on the "document_classification" edge.
func (sdq *ShipmentDocumentationQuery) QueryDocumentClassification() *DocumentClassificationQuery {
	query := (&DocumentClassificationClient{config: sdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentdocumentation.Table, shipmentdocumentation.FieldID, selector),
			sqlgraph.To(documentclassification.Table, documentclassification.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shipmentdocumentation.DocumentClassificationTable, shipmentdocumentation.DocumentClassificationColumn),
		)
		fromU = sqlgraph.SetNeighbors(sdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShipmentDocumentation entity from the query.
// Returns a *NotFoundError when no ShipmentDocumentation was found.
func (sdq *ShipmentDocumentationQuery) First(ctx context.Context) (*ShipmentDocumentation, error) {
	nodes, err := sdq.Limit(1).All(setContextOp(ctx, sdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shipmentdocumentation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) FirstX(ctx context.Context) *ShipmentDocumentation {
	node, err := sdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShipmentDocumentation ID from the query.
// Returns a *NotFoundError when no ShipmentDocumentation ID was found.
func (sdq *ShipmentDocumentationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sdq.Limit(1).IDs(setContextOp(ctx, sdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shipmentdocumentation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := sdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShipmentDocumentation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShipmentDocumentation entity is found.
// Returns a *NotFoundError when no ShipmentDocumentation entities are found.
func (sdq *ShipmentDocumentationQuery) Only(ctx context.Context) (*ShipmentDocumentation, error) {
	nodes, err := sdq.Limit(2).All(setContextOp(ctx, sdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shipmentdocumentation.Label}
	default:
		return nil, &NotSingularError{shipmentdocumentation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) OnlyX(ctx context.Context) *ShipmentDocumentation {
	node, err := sdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShipmentDocumentation ID in the query.
// Returns a *NotSingularError when more than one ShipmentDocumentation ID is found.
// Returns a *NotFoundError when no entities are found.
func (sdq *ShipmentDocumentationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sdq.Limit(2).IDs(setContextOp(ctx, sdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shipmentdocumentation.Label}
	default:
		err = &NotSingularError{shipmentdocumentation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := sdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShipmentDocumentations.
func (sdq *ShipmentDocumentationQuery) All(ctx context.Context) ([]*ShipmentDocumentation, error) {
	ctx = setContextOp(ctx, sdq.ctx, "All")
	if err := sdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShipmentDocumentation, *ShipmentDocumentationQuery]()
	return withInterceptors[[]*ShipmentDocumentation](ctx, sdq, qr, sdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) AllX(ctx context.Context) []*ShipmentDocumentation {
	nodes, err := sdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShipmentDocumentation IDs.
func (sdq *ShipmentDocumentationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if sdq.ctx.Unique == nil && sdq.path != nil {
		sdq.Unique(true)
	}
	ctx = setContextOp(ctx, sdq.ctx, "IDs")
	if err = sdq.Select(shipmentdocumentation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := sdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sdq *ShipmentDocumentationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sdq.ctx, "Count")
	if err := sdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sdq, querierCount[*ShipmentDocumentationQuery](), sdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) CountX(ctx context.Context) int {
	count, err := sdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sdq *ShipmentDocumentationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sdq.ctx, "Exist")
	switch _, err := sdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sdq *ShipmentDocumentationQuery) ExistX(ctx context.Context) bool {
	exist, err := sdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShipmentDocumentationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sdq *ShipmentDocumentationQuery) Clone() *ShipmentDocumentationQuery {
	if sdq == nil {
		return nil
	}
	return &ShipmentDocumentationQuery{
		config:                     sdq.config,
		ctx:                        sdq.ctx.Clone(),
		order:                      append([]shipmentdocumentation.OrderOption{}, sdq.order...),
		inters:                     append([]Interceptor{}, sdq.inters...),
		predicates:                 append([]predicate.ShipmentDocumentation{}, sdq.predicates...),
		withBusinessUnit:           sdq.withBusinessUnit.Clone(),
		withOrganization:           sdq.withOrganization.Clone(),
		withShipment:               sdq.withShipment.Clone(),
		withDocumentClassification: sdq.withDocumentClassification.Clone(),
		// clone intermediate query.
		sql:  sdq.sql.Clone(),
		path: sdq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (sdq *ShipmentDocumentationQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *ShipmentDocumentationQuery {
	query := (&BusinessUnitClient{config: sdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sdq.withBusinessUnit = query
	return sdq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (sdq *ShipmentDocumentationQuery) WithOrganization(opts ...func(*OrganizationQuery)) *ShipmentDocumentationQuery {
	query := (&OrganizationClient{config: sdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sdq.withOrganization = query
	return sdq
}

// WithShipment tells the query-builder to eager-load the nodes that are connected to
// the "shipment" edge. The optional arguments are used to configure the query builder of the edge.
func (sdq *ShipmentDocumentationQuery) WithShipment(opts ...func(*ShipmentQuery)) *ShipmentDocumentationQuery {
	query := (&ShipmentClient{config: sdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sdq.withShipment = query
	return sdq
}

// WithDocumentClassification tells the query-builder to eager-load the nodes that are connected to
// the "document_classification" edge. The optional arguments are used to configure the query builder of the edge.
func (sdq *ShipmentDocumentationQuery) WithDocumentClassification(opts ...func(*DocumentClassificationQuery)) *ShipmentDocumentationQuery {
	query := (&DocumentClassificationClient{config: sdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sdq.withDocumentClassification = query
	return sdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShipmentDocumentation.Query().
//		GroupBy(shipmentdocumentation.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sdq *ShipmentDocumentationQuery) GroupBy(field string, fields ...string) *ShipmentDocumentationGroupBy {
	sdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShipmentDocumentationGroupBy{build: sdq}
	grbuild.flds = &sdq.ctx.Fields
	grbuild.label = shipmentdocumentation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.ShipmentDocumentation.Query().
//		Select(shipmentdocumentation.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (sdq *ShipmentDocumentationQuery) Select(fields ...string) *ShipmentDocumentationSelect {
	sdq.ctx.Fields = append(sdq.ctx.Fields, fields...)
	sbuild := &ShipmentDocumentationSelect{ShipmentDocumentationQuery: sdq}
	sbuild.label = shipmentdocumentation.Label
	sbuild.flds, sbuild.scan = &sdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShipmentDocumentationSelect configured with the given aggregations.
func (sdq *ShipmentDocumentationQuery) Aggregate(fns ...AggregateFunc) *ShipmentDocumentationSelect {
	return sdq.Select().Aggregate(fns...)
}

func (sdq *ShipmentDocumentationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sdq); err != nil {
				return err
			}
		}
	}
	for _, f := range sdq.ctx.Fields {
		if !shipmentdocumentation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sdq.path != nil {
		prev, err := sdq.path(ctx)
		if err != nil {
			return err
		}
		sdq.sql = prev
	}
	return nil
}

func (sdq *ShipmentDocumentationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShipmentDocumentation, error) {
	var (
		nodes       = []*ShipmentDocumentation{}
		_spec       = sdq.querySpec()
		loadedTypes = [4]bool{
			sdq.withBusinessUnit != nil,
			sdq.withOrganization != nil,
			sdq.withShipment != nil,
			sdq.withDocumentClassification != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShipmentDocumentation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShipmentDocumentation{config: sdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(sdq.modifiers) > 0 {
		_spec.Modifiers = sdq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sdq.withBusinessUnit; query != nil {
		if err := sdq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *ShipmentDocumentation, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := sdq.withOrganization; query != nil {
		if err := sdq.loadOrganization(ctx, query, nodes, nil,
			func(n *ShipmentDocumentation, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := sdq.withShipment; query != nil {
		if err := sdq.loadShipment(ctx, query, nodes, nil,
			func(n *ShipmentDocumentation, e *Shipment) { n.Edges.Shipment = e }); err != nil {
			return nil, err
		}
	}
	if query := sdq.withDocumentClassification; query != nil {
		if err := sdq.loadDocumentClassification(ctx, query, nodes, nil,
			func(n *ShipmentDocumentation, e *DocumentClassification) { n.Edges.DocumentClassification = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sdq *ShipmentDocumentationQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*ShipmentDocumentation, init func(*ShipmentDocumentation), assign func(*ShipmentDocumentation, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentDocumentation)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sdq *ShipmentDocumentationQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*ShipmentDocumentation, init func(*ShipmentDocumentation), assign func(*ShipmentDocumentation, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentDocumentation)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sdq *ShipmentDocumentationQuery) loadShipment(ctx context.Context, query *ShipmentQuery, nodes []*ShipmentDocumentation, init func(*ShipmentDocumentation), assign func(*ShipmentDocumentation, *Shipment)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentDocumentation)
	for i := range nodes {
		fk := nodes[i].ShipmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shipment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shipment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sdq *ShipmentDocumentationQuery) loadDocumentClassification(ctx context.Context, query *DocumentClassificationQuery, nodes []*ShipmentDocumentation, init func(*ShipmentDocumentation), assign func(*ShipmentDocumentation, *DocumentClassification)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentDocumentation)
	for i := range nodes {
		fk := nodes[i].DocumentClassificationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(documentclassification.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "document_classification_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sdq *ShipmentDocumentationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sdq.querySpec()
	if len(sdq.modifiers) > 0 {
		_spec.Modifiers = sdq.modifiers
	}
	_spec.Node.Columns = sdq.ctx.Fields
	if len(sdq.ctx.Fields) > 0 {
		_spec.Unique = sdq.ctx.Unique != nil && *sdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sdq.driver, _spec)
}

func (sdq *ShipmentDocumentationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shipmentdocumentation.Table, shipmentdocumentation.Columns, sqlgraph.NewFieldSpec(shipmentdocumentation.FieldID, field.TypeUUID))
	_spec.From = sdq.sql
	if unique := sdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sdq.path != nil {
		_spec.Unique = true
	}
	if fields := sdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shipmentdocumentation.FieldID)
		for i := range fields {
			if fields[i] != shipmentdocumentation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sdq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(shipmentdocumentation.FieldBusinessUnitID)
		}
		if sdq.withOrganization != nil {
			_spec.Node.AddColumnOnce(shipmentdocumentation.FieldOrganizationID)
		}
		if sdq.withShipment != nil {
			_spec.Node.AddColumnOnce(shipmentdocumentation.FieldShipmentID)
		}
		if sdq.withDocumentClassification != nil {
			_spec.Node.AddColumnOnce(shipmentdocumentation.FieldDocumentClassificationID)
		}
	}
	if ps := sdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sdq *ShipmentDocumentationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sdq.driver.Dialect())
	t1 := builder.Table(shipmentdocumentation.Table)
	columns := sdq.ctx.Fields
	if len(columns) == 0 {
		columns = shipmentdocumentation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sdq.sql != nil {
		selector = sdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sdq.ctx.Unique != nil && *sdq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range sdq.modifiers {
		m(selector)
	}
	for _, p := range sdq.predicates {
		p(selector)
	}
	for _, p := range sdq.order {
		p(selector)
	}
	if offset := sdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sdq *ShipmentDocumentationQuery) Modify(modifiers ...func(s *sql.Selector)) *ShipmentDocumentationSelect {
	sdq.modifiers = append(sdq.modifiers, modifiers...)
	return sdq.Select()
}

// ShipmentDocumentationGroupBy is the group-by builder for ShipmentDocumentation entities.
type ShipmentDocumentationGroupBy struct {
	selector
	build *ShipmentDocumentationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sdgb *ShipmentDocumentationGroupBy) Aggregate(fns ...AggregateFunc) *ShipmentDocumentationGroupBy {
	sdgb.fns = append(sdgb.fns, fns...)
	return sdgb
}

// Scan applies the selector query and scans the result into the given value.
func (sdgb *ShipmentDocumentationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sdgb.build.ctx, "GroupBy")
	if err := sdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentDocumentationQuery, *ShipmentDocumentationGroupBy](ctx, sdgb.build, sdgb, sdgb.build.inters, v)
}

func (sdgb *ShipmentDocumentationGroupBy) sqlScan(ctx context.Context, root *ShipmentDocumentationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sdgb.fns))
	for _, fn := range sdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sdgb.flds)+len(sdgb.fns))
		for _, f := range *sdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShipmentDocumentationSelect is the builder for selecting fields of ShipmentDocumentation entities.
type ShipmentDocumentationSelect struct {
	*ShipmentDocumentationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sds *ShipmentDocumentationSelect) Aggregate(fns ...AggregateFunc) *ShipmentDocumentationSelect {
	sds.fns = append(sds.fns, fns...)
	return sds
}

// Scan applies the selector query and scans the result into the given value.
func (sds *ShipmentDocumentationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sds.ctx, "Select")
	if err := sds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentDocumentationQuery, *ShipmentDocumentationSelect](ctx, sds.ShipmentDocumentationQuery, sds, sds.inters, v)
}

func (sds *ShipmentDocumentationSelect) sqlScan(ctx context.Context, root *ShipmentDocumentationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sds.fns))
	for _, fn := range sds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sds *ShipmentDocumentationSelect) Modify(modifiers ...func(s *sql.Selector)) *ShipmentDocumentationSelect {
	sds.modifiers = append(sds.modifiers, modifiers...)
	return sds
}
