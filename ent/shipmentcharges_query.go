// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/ent/accessorialcharge"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/emoss08/trenova/ent/predicate"
	"github.com/emoss08/trenova/ent/shipment"
	"github.com/emoss08/trenova/ent/shipmentcharges"
	"github.com/emoss08/trenova/ent/user"
	"github.com/google/uuid"
)

// ShipmentChargesQuery is the builder for querying ShipmentCharges entities.
type ShipmentChargesQuery struct {
	config
	ctx                   *QueryContext
	order                 []shipmentcharges.OrderOption
	inters                []Interceptor
	predicates            []predicate.ShipmentCharges
	withBusinessUnit      *BusinessUnitQuery
	withOrganization      *OrganizationQuery
	withShipment          *ShipmentQuery
	withAccessorialCharge *AccessorialChargeQuery
	withUser              *UserQuery
	modifiers             []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShipmentChargesQuery builder.
func (scq *ShipmentChargesQuery) Where(ps ...predicate.ShipmentCharges) *ShipmentChargesQuery {
	scq.predicates = append(scq.predicates, ps...)
	return scq
}

// Limit the number of records to be returned by this query.
func (scq *ShipmentChargesQuery) Limit(limit int) *ShipmentChargesQuery {
	scq.ctx.Limit = &limit
	return scq
}

// Offset to start from.
func (scq *ShipmentChargesQuery) Offset(offset int) *ShipmentChargesQuery {
	scq.ctx.Offset = &offset
	return scq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scq *ShipmentChargesQuery) Unique(unique bool) *ShipmentChargesQuery {
	scq.ctx.Unique = &unique
	return scq
}

// Order specifies how the records should be ordered.
func (scq *ShipmentChargesQuery) Order(o ...shipmentcharges.OrderOption) *ShipmentChargesQuery {
	scq.order = append(scq.order, o...)
	return scq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (scq *ShipmentChargesQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentcharges.Table, shipmentcharges.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmentcharges.BusinessUnitTable, shipmentcharges.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (scq *ShipmentChargesQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentcharges.Table, shipmentcharges.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmentcharges.OrganizationTable, shipmentcharges.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShipment chains the current query on the "shipment" edge.
func (scq *ShipmentChargesQuery) QueryShipment() *ShipmentQuery {
	query := (&ShipmentClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentcharges.Table, shipmentcharges.FieldID, selector),
			sqlgraph.To(shipment.Table, shipment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shipmentcharges.ShipmentTable, shipmentcharges.ShipmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAccessorialCharge chains the current query on the "accessorial_charge" edge.
func (scq *ShipmentChargesQuery) QueryAccessorialCharge() *AccessorialChargeQuery {
	query := (&AccessorialChargeClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentcharges.Table, shipmentcharges.FieldID, selector),
			sqlgraph.To(accessorialcharge.Table, accessorialcharge.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shipmentcharges.AccessorialChargeTable, shipmentcharges.AccessorialChargeColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (scq *ShipmentChargesQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentcharges.Table, shipmentcharges.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shipmentcharges.UserTable, shipmentcharges.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShipmentCharges entity from the query.
// Returns a *NotFoundError when no ShipmentCharges was found.
func (scq *ShipmentChargesQuery) First(ctx context.Context) (*ShipmentCharges, error) {
	nodes, err := scq.Limit(1).All(setContextOp(ctx, scq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shipmentcharges.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scq *ShipmentChargesQuery) FirstX(ctx context.Context) *ShipmentCharges {
	node, err := scq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShipmentCharges ID from the query.
// Returns a *NotFoundError when no ShipmentCharges ID was found.
func (scq *ShipmentChargesQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = scq.Limit(1).IDs(setContextOp(ctx, scq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shipmentcharges.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scq *ShipmentChargesQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := scq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShipmentCharges entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShipmentCharges entity is found.
// Returns a *NotFoundError when no ShipmentCharges entities are found.
func (scq *ShipmentChargesQuery) Only(ctx context.Context) (*ShipmentCharges, error) {
	nodes, err := scq.Limit(2).All(setContextOp(ctx, scq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shipmentcharges.Label}
	default:
		return nil, &NotSingularError{shipmentcharges.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scq *ShipmentChargesQuery) OnlyX(ctx context.Context) *ShipmentCharges {
	node, err := scq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShipmentCharges ID in the query.
// Returns a *NotSingularError when more than one ShipmentCharges ID is found.
// Returns a *NotFoundError when no entities are found.
func (scq *ShipmentChargesQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = scq.Limit(2).IDs(setContextOp(ctx, scq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shipmentcharges.Label}
	default:
		err = &NotSingularError{shipmentcharges.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scq *ShipmentChargesQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := scq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShipmentChargesSlice.
func (scq *ShipmentChargesQuery) All(ctx context.Context) ([]*ShipmentCharges, error) {
	ctx = setContextOp(ctx, scq.ctx, "All")
	if err := scq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShipmentCharges, *ShipmentChargesQuery]()
	return withInterceptors[[]*ShipmentCharges](ctx, scq, qr, scq.inters)
}

// AllX is like All, but panics if an error occurs.
func (scq *ShipmentChargesQuery) AllX(ctx context.Context) []*ShipmentCharges {
	nodes, err := scq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShipmentCharges IDs.
func (scq *ShipmentChargesQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if scq.ctx.Unique == nil && scq.path != nil {
		scq.Unique(true)
	}
	ctx = setContextOp(ctx, scq.ctx, "IDs")
	if err = scq.Select(shipmentcharges.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scq *ShipmentChargesQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := scq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scq *ShipmentChargesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, scq.ctx, "Count")
	if err := scq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, scq, querierCount[*ShipmentChargesQuery](), scq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (scq *ShipmentChargesQuery) CountX(ctx context.Context) int {
	count, err := scq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scq *ShipmentChargesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, scq.ctx, "Exist")
	switch _, err := scq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (scq *ShipmentChargesQuery) ExistX(ctx context.Context) bool {
	exist, err := scq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShipmentChargesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scq *ShipmentChargesQuery) Clone() *ShipmentChargesQuery {
	if scq == nil {
		return nil
	}
	return &ShipmentChargesQuery{
		config:                scq.config,
		ctx:                   scq.ctx.Clone(),
		order:                 append([]shipmentcharges.OrderOption{}, scq.order...),
		inters:                append([]Interceptor{}, scq.inters...),
		predicates:            append([]predicate.ShipmentCharges{}, scq.predicates...),
		withBusinessUnit:      scq.withBusinessUnit.Clone(),
		withOrganization:      scq.withOrganization.Clone(),
		withShipment:          scq.withShipment.Clone(),
		withAccessorialCharge: scq.withAccessorialCharge.Clone(),
		withUser:              scq.withUser.Clone(),
		// clone intermediate query.
		sql:  scq.sql.Clone(),
		path: scq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *ShipmentChargesQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *ShipmentChargesQuery {
	query := (&BusinessUnitClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withBusinessUnit = query
	return scq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *ShipmentChargesQuery) WithOrganization(opts ...func(*OrganizationQuery)) *ShipmentChargesQuery {
	query := (&OrganizationClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withOrganization = query
	return scq
}

// WithShipment tells the query-builder to eager-load the nodes that are connected to
// the "shipment" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *ShipmentChargesQuery) WithShipment(opts ...func(*ShipmentQuery)) *ShipmentChargesQuery {
	query := (&ShipmentClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withShipment = query
	return scq
}

// WithAccessorialCharge tells the query-builder to eager-load the nodes that are connected to
// the "accessorial_charge" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *ShipmentChargesQuery) WithAccessorialCharge(opts ...func(*AccessorialChargeQuery)) *ShipmentChargesQuery {
	query := (&AccessorialChargeClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withAccessorialCharge = query
	return scq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *ShipmentChargesQuery) WithUser(opts ...func(*UserQuery)) *ShipmentChargesQuery {
	query := (&UserClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withUser = query
	return scq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShipmentCharges.Query().
//		GroupBy(shipmentcharges.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scq *ShipmentChargesQuery) GroupBy(field string, fields ...string) *ShipmentChargesGroupBy {
	scq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShipmentChargesGroupBy{build: scq}
	grbuild.flds = &scq.ctx.Fields
	grbuild.label = shipmentcharges.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.ShipmentCharges.Query().
//		Select(shipmentcharges.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (scq *ShipmentChargesQuery) Select(fields ...string) *ShipmentChargesSelect {
	scq.ctx.Fields = append(scq.ctx.Fields, fields...)
	sbuild := &ShipmentChargesSelect{ShipmentChargesQuery: scq}
	sbuild.label = shipmentcharges.Label
	sbuild.flds, sbuild.scan = &scq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShipmentChargesSelect configured with the given aggregations.
func (scq *ShipmentChargesQuery) Aggregate(fns ...AggregateFunc) *ShipmentChargesSelect {
	return scq.Select().Aggregate(fns...)
}

func (scq *ShipmentChargesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range scq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, scq); err != nil {
				return err
			}
		}
	}
	for _, f := range scq.ctx.Fields {
		if !shipmentcharges.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scq.path != nil {
		prev, err := scq.path(ctx)
		if err != nil {
			return err
		}
		scq.sql = prev
	}
	return nil
}

func (scq *ShipmentChargesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShipmentCharges, error) {
	var (
		nodes       = []*ShipmentCharges{}
		_spec       = scq.querySpec()
		loadedTypes = [5]bool{
			scq.withBusinessUnit != nil,
			scq.withOrganization != nil,
			scq.withShipment != nil,
			scq.withAccessorialCharge != nil,
			scq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShipmentCharges).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShipmentCharges{config: scq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(scq.modifiers) > 0 {
		_spec.Modifiers = scq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := scq.withBusinessUnit; query != nil {
		if err := scq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *ShipmentCharges, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withOrganization; query != nil {
		if err := scq.loadOrganization(ctx, query, nodes, nil,
			func(n *ShipmentCharges, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withShipment; query != nil {
		if err := scq.loadShipment(ctx, query, nodes, nil,
			func(n *ShipmentCharges, e *Shipment) { n.Edges.Shipment = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withAccessorialCharge; query != nil {
		if err := scq.loadAccessorialCharge(ctx, query, nodes, nil,
			func(n *ShipmentCharges, e *AccessorialCharge) { n.Edges.AccessorialCharge = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withUser; query != nil {
		if err := scq.loadUser(ctx, query, nodes, nil,
			func(n *ShipmentCharges, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (scq *ShipmentChargesQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*ShipmentCharges, init func(*ShipmentCharges), assign func(*ShipmentCharges, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentCharges)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *ShipmentChargesQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*ShipmentCharges, init func(*ShipmentCharges), assign func(*ShipmentCharges, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentCharges)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *ShipmentChargesQuery) loadShipment(ctx context.Context, query *ShipmentQuery, nodes []*ShipmentCharges, init func(*ShipmentCharges), assign func(*ShipmentCharges, *Shipment)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentCharges)
	for i := range nodes {
		fk := nodes[i].ShipmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shipment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shipment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *ShipmentChargesQuery) loadAccessorialCharge(ctx context.Context, query *AccessorialChargeQuery, nodes []*ShipmentCharges, init func(*ShipmentCharges), assign func(*ShipmentCharges, *AccessorialCharge)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentCharges)
	for i := range nodes {
		fk := nodes[i].AccessorialChargeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(accessorialcharge.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "accessorial_charge_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *ShipmentChargesQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ShipmentCharges, init func(*ShipmentCharges), assign func(*ShipmentCharges, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentCharges)
	for i := range nodes {
		fk := nodes[i].CreatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (scq *ShipmentChargesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scq.querySpec()
	if len(scq.modifiers) > 0 {
		_spec.Modifiers = scq.modifiers
	}
	_spec.Node.Columns = scq.ctx.Fields
	if len(scq.ctx.Fields) > 0 {
		_spec.Unique = scq.ctx.Unique != nil && *scq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, scq.driver, _spec)
}

func (scq *ShipmentChargesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shipmentcharges.Table, shipmentcharges.Columns, sqlgraph.NewFieldSpec(shipmentcharges.FieldID, field.TypeUUID))
	_spec.From = scq.sql
	if unique := scq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if scq.path != nil {
		_spec.Unique = true
	}
	if fields := scq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shipmentcharges.FieldID)
		for i := range fields {
			if fields[i] != shipmentcharges.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if scq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(shipmentcharges.FieldBusinessUnitID)
		}
		if scq.withOrganization != nil {
			_spec.Node.AddColumnOnce(shipmentcharges.FieldOrganizationID)
		}
		if scq.withShipment != nil {
			_spec.Node.AddColumnOnce(shipmentcharges.FieldShipmentID)
		}
		if scq.withAccessorialCharge != nil {
			_spec.Node.AddColumnOnce(shipmentcharges.FieldAccessorialChargeID)
		}
		if scq.withUser != nil {
			_spec.Node.AddColumnOnce(shipmentcharges.FieldCreatedBy)
		}
	}
	if ps := scq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scq *ShipmentChargesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scq.driver.Dialect())
	t1 := builder.Table(shipmentcharges.Table)
	columns := scq.ctx.Fields
	if len(columns) == 0 {
		columns = shipmentcharges.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scq.sql != nil {
		selector = scq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scq.ctx.Unique != nil && *scq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range scq.modifiers {
		m(selector)
	}
	for _, p := range scq.predicates {
		p(selector)
	}
	for _, p := range scq.order {
		p(selector)
	}
	if offset := scq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (scq *ShipmentChargesQuery) Modify(modifiers ...func(s *sql.Selector)) *ShipmentChargesSelect {
	scq.modifiers = append(scq.modifiers, modifiers...)
	return scq.Select()
}

// ShipmentChargesGroupBy is the group-by builder for ShipmentCharges entities.
type ShipmentChargesGroupBy struct {
	selector
	build *ShipmentChargesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scgb *ShipmentChargesGroupBy) Aggregate(fns ...AggregateFunc) *ShipmentChargesGroupBy {
	scgb.fns = append(scgb.fns, fns...)
	return scgb
}

// Scan applies the selector query and scans the result into the given value.
func (scgb *ShipmentChargesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scgb.build.ctx, "GroupBy")
	if err := scgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentChargesQuery, *ShipmentChargesGroupBy](ctx, scgb.build, scgb, scgb.build.inters, v)
}

func (scgb *ShipmentChargesGroupBy) sqlScan(ctx context.Context, root *ShipmentChargesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scgb.fns))
	for _, fn := range scgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scgb.flds)+len(scgb.fns))
		for _, f := range *scgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShipmentChargesSelect is the builder for selecting fields of ShipmentCharges entities.
type ShipmentChargesSelect struct {
	*ShipmentChargesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scs *ShipmentChargesSelect) Aggregate(fns ...AggregateFunc) *ShipmentChargesSelect {
	scs.fns = append(scs.fns, fns...)
	return scs
}

// Scan applies the selector query and scans the result into the given value.
func (scs *ShipmentChargesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scs.ctx, "Select")
	if err := scs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentChargesQuery, *ShipmentChargesSelect](ctx, scs.ShipmentChargesQuery, scs, scs.inters, v)
}

func (scs *ShipmentChargesSelect) sqlScan(ctx context.Context, root *ShipmentChargesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scs.fns))
	for _, fn := range scs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (scs *ShipmentChargesSelect) Modify(modifiers ...func(s *sql.Selector)) *ShipmentChargesSelect {
	scs.modifiers = append(scs.modifiers, modifiers...)
	return scs
}
