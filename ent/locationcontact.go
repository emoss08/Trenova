// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/location"
	"github.com/emoss08/trenova/ent/locationcontact"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/google/uuid"
)

// LocationContact is the model entity for the LocationContact schema.
type LocationContact struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BusinessUnitID holds the value of the "business_unit_id" field.
	BusinessUnitID uuid.UUID `json:"businessUnitId"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organizationId"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updatedAt"`
	// Version holds the value of the "version" field.
	Version int `json:"version" validate:"omitempty"`
	// LocationID holds the value of the "location_id" field.
	LocationID uuid.UUID `json:"locationId" validate:"required"`
	// Name holds the value of the "name" field.
	Name string `json:"name" validate:"required"`
	// EmailAddress holds the value of the "email_address" field.
	EmailAddress string `json:"emailAddress" validate:"omitempty,email"`
	// PhoneNumber holds the value of the "phone_number" field.
	PhoneNumber string `json:"phoneNumber" validate:"omitempty,phone"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LocationContactQuery when eager-loading is set.
	Edges        LocationContactEdges `json:"edges"`
	selectValues sql.SelectValues
}

// LocationContactEdges holds the relations/edges for other nodes in the graph.
type LocationContactEdges struct {
	// BusinessUnit holds the value of the business_unit edge.
	BusinessUnit *BusinessUnit `json:"business_unit,omitempty"`
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// Location holds the value of the location edge.
	Location *Location `json:"location,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// BusinessUnitOrErr returns the BusinessUnit value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LocationContactEdges) BusinessUnitOrErr() (*BusinessUnit, error) {
	if e.BusinessUnit != nil {
		return e.BusinessUnit, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: businessunit.Label}
	}
	return nil, &NotLoadedError{edge: "business_unit"}
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LocationContactEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// LocationOrErr returns the Location value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LocationContactEdges) LocationOrErr() (*Location, error) {
	if e.Location != nil {
		return e.Location, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: location.Label}
	}
	return nil, &NotLoadedError{edge: "location"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LocationContact) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case locationcontact.FieldVersion:
			values[i] = new(sql.NullInt64)
		case locationcontact.FieldName, locationcontact.FieldEmailAddress, locationcontact.FieldPhoneNumber:
			values[i] = new(sql.NullString)
		case locationcontact.FieldCreatedAt, locationcontact.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case locationcontact.FieldID, locationcontact.FieldBusinessUnitID, locationcontact.FieldOrganizationID, locationcontact.FieldLocationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LocationContact fields.
func (lc *LocationContact) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case locationcontact.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				lc.ID = *value
			}
		case locationcontact.FieldBusinessUnitID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field business_unit_id", values[i])
			} else if value != nil {
				lc.BusinessUnitID = *value
			}
		case locationcontact.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				lc.OrganizationID = *value
			}
		case locationcontact.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				lc.CreatedAt = value.Time
			}
		case locationcontact.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				lc.UpdatedAt = value.Time
			}
		case locationcontact.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				lc.Version = int(value.Int64)
			}
		case locationcontact.FieldLocationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field location_id", values[i])
			} else if value != nil {
				lc.LocationID = *value
			}
		case locationcontact.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				lc.Name = value.String
			}
		case locationcontact.FieldEmailAddress:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email_address", values[i])
			} else if value.Valid {
				lc.EmailAddress = value.String
			}
		case locationcontact.FieldPhoneNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field phone_number", values[i])
			} else if value.Valid {
				lc.PhoneNumber = value.String
			}
		default:
			lc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LocationContact.
// This includes values selected through modifiers, order, etc.
func (lc *LocationContact) Value(name string) (ent.Value, error) {
	return lc.selectValues.Get(name)
}

// QueryBusinessUnit queries the "business_unit" edge of the LocationContact entity.
func (lc *LocationContact) QueryBusinessUnit() *BusinessUnitQuery {
	return NewLocationContactClient(lc.config).QueryBusinessUnit(lc)
}

// QueryOrganization queries the "organization" edge of the LocationContact entity.
func (lc *LocationContact) QueryOrganization() *OrganizationQuery {
	return NewLocationContactClient(lc.config).QueryOrganization(lc)
}

// QueryLocation queries the "location" edge of the LocationContact entity.
func (lc *LocationContact) QueryLocation() *LocationQuery {
	return NewLocationContactClient(lc.config).QueryLocation(lc)
}

// Update returns a builder for updating this LocationContact.
// Note that you need to call LocationContact.Unwrap() before calling this method if this LocationContact
// was returned from a transaction, and the transaction was committed or rolled back.
func (lc *LocationContact) Update() *LocationContactUpdateOne {
	return NewLocationContactClient(lc.config).UpdateOne(lc)
}

// Unwrap unwraps the LocationContact entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (lc *LocationContact) Unwrap() *LocationContact {
	_tx, ok := lc.config.driver.(*txDriver)
	if !ok {
		panic("ent: LocationContact is not a transactional entity")
	}
	lc.config.driver = _tx.drv
	return lc
}

// String implements the fmt.Stringer.
func (lc *LocationContact) String() string {
	var builder strings.Builder
	builder.WriteString("LocationContact(")
	builder.WriteString(fmt.Sprintf("id=%v, ", lc.ID))
	builder.WriteString("business_unit_id=")
	builder.WriteString(fmt.Sprintf("%v", lc.BusinessUnitID))
	builder.WriteString(", ")
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", lc.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(lc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(lc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", lc.Version))
	builder.WriteString(", ")
	builder.WriteString("location_id=")
	builder.WriteString(fmt.Sprintf("%v", lc.LocationID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(lc.Name)
	builder.WriteString(", ")
	builder.WriteString("email_address=")
	builder.WriteString(lc.EmailAddress)
	builder.WriteString(", ")
	builder.WriteString("phone_number=")
	builder.WriteString(lc.PhoneNumber)
	builder.WriteByte(')')
	return builder.String()
}

// LocationContacts is a parsable slice of LocationContact.
type LocationContacts []*LocationContact
