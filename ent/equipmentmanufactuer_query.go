// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/equipmentmanufactuer"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/emoss08/trenova/ent/predicate"
	"github.com/google/uuid"
)

// EquipmentManufactuerQuery is the builder for querying EquipmentManufactuer entities.
type EquipmentManufactuerQuery struct {
	config
	ctx              *QueryContext
	order            []equipmentmanufactuer.OrderOption
	inters           []Interceptor
	predicates       []predicate.EquipmentManufactuer
	withBusinessUnit *BusinessUnitQuery
	withOrganization *OrganizationQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EquipmentManufactuerQuery builder.
func (emq *EquipmentManufactuerQuery) Where(ps ...predicate.EquipmentManufactuer) *EquipmentManufactuerQuery {
	emq.predicates = append(emq.predicates, ps...)
	return emq
}

// Limit the number of records to be returned by this query.
func (emq *EquipmentManufactuerQuery) Limit(limit int) *EquipmentManufactuerQuery {
	emq.ctx.Limit = &limit
	return emq
}

// Offset to start from.
func (emq *EquipmentManufactuerQuery) Offset(offset int) *EquipmentManufactuerQuery {
	emq.ctx.Offset = &offset
	return emq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (emq *EquipmentManufactuerQuery) Unique(unique bool) *EquipmentManufactuerQuery {
	emq.ctx.Unique = &unique
	return emq
}

// Order specifies how the records should be ordered.
func (emq *EquipmentManufactuerQuery) Order(o ...equipmentmanufactuer.OrderOption) *EquipmentManufactuerQuery {
	emq.order = append(emq.order, o...)
	return emq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (emq *EquipmentManufactuerQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: emq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := emq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := emq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(equipmentmanufactuer.Table, equipmentmanufactuer.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, equipmentmanufactuer.BusinessUnitTable, equipmentmanufactuer.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(emq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (emq *EquipmentManufactuerQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: emq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := emq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := emq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(equipmentmanufactuer.Table, equipmentmanufactuer.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, equipmentmanufactuer.OrganizationTable, equipmentmanufactuer.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(emq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EquipmentManufactuer entity from the query.
// Returns a *NotFoundError when no EquipmentManufactuer was found.
func (emq *EquipmentManufactuerQuery) First(ctx context.Context) (*EquipmentManufactuer, error) {
	nodes, err := emq.Limit(1).All(setContextOp(ctx, emq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{equipmentmanufactuer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) FirstX(ctx context.Context) *EquipmentManufactuer {
	node, err := emq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EquipmentManufactuer ID from the query.
// Returns a *NotFoundError when no EquipmentManufactuer ID was found.
func (emq *EquipmentManufactuerQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = emq.Limit(1).IDs(setContextOp(ctx, emq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{equipmentmanufactuer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := emq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EquipmentManufactuer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EquipmentManufactuer entity is found.
// Returns a *NotFoundError when no EquipmentManufactuer entities are found.
func (emq *EquipmentManufactuerQuery) Only(ctx context.Context) (*EquipmentManufactuer, error) {
	nodes, err := emq.Limit(2).All(setContextOp(ctx, emq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{equipmentmanufactuer.Label}
	default:
		return nil, &NotSingularError{equipmentmanufactuer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) OnlyX(ctx context.Context) *EquipmentManufactuer {
	node, err := emq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EquipmentManufactuer ID in the query.
// Returns a *NotSingularError when more than one EquipmentManufactuer ID is found.
// Returns a *NotFoundError when no entities are found.
func (emq *EquipmentManufactuerQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = emq.Limit(2).IDs(setContextOp(ctx, emq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{equipmentmanufactuer.Label}
	default:
		err = &NotSingularError{equipmentmanufactuer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := emq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EquipmentManufactuers.
func (emq *EquipmentManufactuerQuery) All(ctx context.Context) ([]*EquipmentManufactuer, error) {
	ctx = setContextOp(ctx, emq.ctx, "All")
	if err := emq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EquipmentManufactuer, *EquipmentManufactuerQuery]()
	return withInterceptors[[]*EquipmentManufactuer](ctx, emq, qr, emq.inters)
}

// AllX is like All, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) AllX(ctx context.Context) []*EquipmentManufactuer {
	nodes, err := emq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EquipmentManufactuer IDs.
func (emq *EquipmentManufactuerQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if emq.ctx.Unique == nil && emq.path != nil {
		emq.Unique(true)
	}
	ctx = setContextOp(ctx, emq.ctx, "IDs")
	if err = emq.Select(equipmentmanufactuer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := emq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (emq *EquipmentManufactuerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, emq.ctx, "Count")
	if err := emq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, emq, querierCount[*EquipmentManufactuerQuery](), emq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) CountX(ctx context.Context) int {
	count, err := emq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (emq *EquipmentManufactuerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, emq.ctx, "Exist")
	switch _, err := emq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (emq *EquipmentManufactuerQuery) ExistX(ctx context.Context) bool {
	exist, err := emq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EquipmentManufactuerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (emq *EquipmentManufactuerQuery) Clone() *EquipmentManufactuerQuery {
	if emq == nil {
		return nil
	}
	return &EquipmentManufactuerQuery{
		config:           emq.config,
		ctx:              emq.ctx.Clone(),
		order:            append([]equipmentmanufactuer.OrderOption{}, emq.order...),
		inters:           append([]Interceptor{}, emq.inters...),
		predicates:       append([]predicate.EquipmentManufactuer{}, emq.predicates...),
		withBusinessUnit: emq.withBusinessUnit.Clone(),
		withOrganization: emq.withOrganization.Clone(),
		// clone intermediate query.
		sql:  emq.sql.Clone(),
		path: emq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (emq *EquipmentManufactuerQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *EquipmentManufactuerQuery {
	query := (&BusinessUnitClient{config: emq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	emq.withBusinessUnit = query
	return emq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (emq *EquipmentManufactuerQuery) WithOrganization(opts ...func(*OrganizationQuery)) *EquipmentManufactuerQuery {
	query := (&OrganizationClient{config: emq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	emq.withOrganization = query
	return emq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EquipmentManufactuer.Query().
//		GroupBy(equipmentmanufactuer.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (emq *EquipmentManufactuerQuery) GroupBy(field string, fields ...string) *EquipmentManufactuerGroupBy {
	emq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EquipmentManufactuerGroupBy{build: emq}
	grbuild.flds = &emq.ctx.Fields
	grbuild.label = equipmentmanufactuer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.EquipmentManufactuer.Query().
//		Select(equipmentmanufactuer.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (emq *EquipmentManufactuerQuery) Select(fields ...string) *EquipmentManufactuerSelect {
	emq.ctx.Fields = append(emq.ctx.Fields, fields...)
	sbuild := &EquipmentManufactuerSelect{EquipmentManufactuerQuery: emq}
	sbuild.label = equipmentmanufactuer.Label
	sbuild.flds, sbuild.scan = &emq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EquipmentManufactuerSelect configured with the given aggregations.
func (emq *EquipmentManufactuerQuery) Aggregate(fns ...AggregateFunc) *EquipmentManufactuerSelect {
	return emq.Select().Aggregate(fns...)
}

func (emq *EquipmentManufactuerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range emq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, emq); err != nil {
				return err
			}
		}
	}
	for _, f := range emq.ctx.Fields {
		if !equipmentmanufactuer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if emq.path != nil {
		prev, err := emq.path(ctx)
		if err != nil {
			return err
		}
		emq.sql = prev
	}
	return nil
}

func (emq *EquipmentManufactuerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EquipmentManufactuer, error) {
	var (
		nodes       = []*EquipmentManufactuer{}
		_spec       = emq.querySpec()
		loadedTypes = [2]bool{
			emq.withBusinessUnit != nil,
			emq.withOrganization != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EquipmentManufactuer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EquipmentManufactuer{config: emq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(emq.modifiers) > 0 {
		_spec.Modifiers = emq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, emq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := emq.withBusinessUnit; query != nil {
		if err := emq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *EquipmentManufactuer, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := emq.withOrganization; query != nil {
		if err := emq.loadOrganization(ctx, query, nodes, nil,
			func(n *EquipmentManufactuer, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (emq *EquipmentManufactuerQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*EquipmentManufactuer, init func(*EquipmentManufactuer), assign func(*EquipmentManufactuer, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*EquipmentManufactuer)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (emq *EquipmentManufactuerQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*EquipmentManufactuer, init func(*EquipmentManufactuer), assign func(*EquipmentManufactuer, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*EquipmentManufactuer)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (emq *EquipmentManufactuerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := emq.querySpec()
	if len(emq.modifiers) > 0 {
		_spec.Modifiers = emq.modifiers
	}
	_spec.Node.Columns = emq.ctx.Fields
	if len(emq.ctx.Fields) > 0 {
		_spec.Unique = emq.ctx.Unique != nil && *emq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, emq.driver, _spec)
}

func (emq *EquipmentManufactuerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(equipmentmanufactuer.Table, equipmentmanufactuer.Columns, sqlgraph.NewFieldSpec(equipmentmanufactuer.FieldID, field.TypeUUID))
	_spec.From = emq.sql
	if unique := emq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if emq.path != nil {
		_spec.Unique = true
	}
	if fields := emq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, equipmentmanufactuer.FieldID)
		for i := range fields {
			if fields[i] != equipmentmanufactuer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if emq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(equipmentmanufactuer.FieldBusinessUnitID)
		}
		if emq.withOrganization != nil {
			_spec.Node.AddColumnOnce(equipmentmanufactuer.FieldOrganizationID)
		}
	}
	if ps := emq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := emq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := emq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := emq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (emq *EquipmentManufactuerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(emq.driver.Dialect())
	t1 := builder.Table(equipmentmanufactuer.Table)
	columns := emq.ctx.Fields
	if len(columns) == 0 {
		columns = equipmentmanufactuer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if emq.sql != nil {
		selector = emq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if emq.ctx.Unique != nil && *emq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range emq.modifiers {
		m(selector)
	}
	for _, p := range emq.predicates {
		p(selector)
	}
	for _, p := range emq.order {
		p(selector)
	}
	if offset := emq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := emq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (emq *EquipmentManufactuerQuery) Modify(modifiers ...func(s *sql.Selector)) *EquipmentManufactuerSelect {
	emq.modifiers = append(emq.modifiers, modifiers...)
	return emq.Select()
}

// EquipmentManufactuerGroupBy is the group-by builder for EquipmentManufactuer entities.
type EquipmentManufactuerGroupBy struct {
	selector
	build *EquipmentManufactuerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (emgb *EquipmentManufactuerGroupBy) Aggregate(fns ...AggregateFunc) *EquipmentManufactuerGroupBy {
	emgb.fns = append(emgb.fns, fns...)
	return emgb
}

// Scan applies the selector query and scans the result into the given value.
func (emgb *EquipmentManufactuerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, emgb.build.ctx, "GroupBy")
	if err := emgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipmentManufactuerQuery, *EquipmentManufactuerGroupBy](ctx, emgb.build, emgb, emgb.build.inters, v)
}

func (emgb *EquipmentManufactuerGroupBy) sqlScan(ctx context.Context, root *EquipmentManufactuerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(emgb.fns))
	for _, fn := range emgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*emgb.flds)+len(emgb.fns))
		for _, f := range *emgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*emgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := emgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EquipmentManufactuerSelect is the builder for selecting fields of EquipmentManufactuer entities.
type EquipmentManufactuerSelect struct {
	*EquipmentManufactuerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ems *EquipmentManufactuerSelect) Aggregate(fns ...AggregateFunc) *EquipmentManufactuerSelect {
	ems.fns = append(ems.fns, fns...)
	return ems
}

// Scan applies the selector query and scans the result into the given value.
func (ems *EquipmentManufactuerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ems.ctx, "Select")
	if err := ems.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipmentManufactuerQuery, *EquipmentManufactuerSelect](ctx, ems.EquipmentManufactuerQuery, ems, ems.inters, v)
}

func (ems *EquipmentManufactuerSelect) sqlScan(ctx context.Context, root *EquipmentManufactuerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ems.fns))
	for _, fn := range ems.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ems.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ems.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ems *EquipmentManufactuerSelect) Modify(modifiers ...func(s *sql.Selector)) *EquipmentManufactuerSelect {
	ems.modifiers = append(ems.modifiers, modifiers...)
	return ems
}
