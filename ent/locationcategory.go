// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/emoss08/trenova/ent/businessunit"
	"github.com/emoss08/trenova/ent/locationcategory"
	"github.com/emoss08/trenova/ent/organization"
	"github.com/google/uuid"
)

// LocationCategory is the model entity for the LocationCategory schema.
type LocationCategory struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BusinessUnitID holds the value of the "business_unit_id" field.
	BusinessUnitID uuid.UUID `json:"businessUnitId"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organizationId"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updatedAt"`
	// Name holds the value of the "name" field.
	Name string `json:"name" validate:"required"`
	// Description holds the value of the "description" field.
	Description string `json:"description" validate:"omitempty"`
	// Color holds the value of the "color" field.
	Color string `json:"color" validate:"omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LocationCategoryQuery when eager-loading is set.
	Edges        LocationCategoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// LocationCategoryEdges holds the relations/edges for other nodes in the graph.
type LocationCategoryEdges struct {
	// BusinessUnit holds the value of the business_unit edge.
	BusinessUnit *BusinessUnit `json:"business_unit,omitempty"`
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// BusinessUnitOrErr returns the BusinessUnit value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LocationCategoryEdges) BusinessUnitOrErr() (*BusinessUnit, error) {
	if e.BusinessUnit != nil {
		return e.BusinessUnit, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: businessunit.Label}
	}
	return nil, &NotLoadedError{edge: "business_unit"}
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LocationCategoryEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LocationCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case locationcategory.FieldName, locationcategory.FieldDescription, locationcategory.FieldColor:
			values[i] = new(sql.NullString)
		case locationcategory.FieldCreatedAt, locationcategory.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case locationcategory.FieldID, locationcategory.FieldBusinessUnitID, locationcategory.FieldOrganizationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LocationCategory fields.
func (lc *LocationCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case locationcategory.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				lc.ID = *value
			}
		case locationcategory.FieldBusinessUnitID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field business_unit_id", values[i])
			} else if value != nil {
				lc.BusinessUnitID = *value
			}
		case locationcategory.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				lc.OrganizationID = *value
			}
		case locationcategory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				lc.CreatedAt = value.Time
			}
		case locationcategory.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				lc.UpdatedAt = value.Time
			}
		case locationcategory.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				lc.Name = value.String
			}
		case locationcategory.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				lc.Description = value.String
			}
		case locationcategory.FieldColor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field color", values[i])
			} else if value.Valid {
				lc.Color = value.String
			}
		default:
			lc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LocationCategory.
// This includes values selected through modifiers, order, etc.
func (lc *LocationCategory) Value(name string) (ent.Value, error) {
	return lc.selectValues.Get(name)
}

// QueryBusinessUnit queries the "business_unit" edge of the LocationCategory entity.
func (lc *LocationCategory) QueryBusinessUnit() *BusinessUnitQuery {
	return NewLocationCategoryClient(lc.config).QueryBusinessUnit(lc)
}

// QueryOrganization queries the "organization" edge of the LocationCategory entity.
func (lc *LocationCategory) QueryOrganization() *OrganizationQuery {
	return NewLocationCategoryClient(lc.config).QueryOrganization(lc)
}

// Update returns a builder for updating this LocationCategory.
// Note that you need to call LocationCategory.Unwrap() before calling this method if this LocationCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (lc *LocationCategory) Update() *LocationCategoryUpdateOne {
	return NewLocationCategoryClient(lc.config).UpdateOne(lc)
}

// Unwrap unwraps the LocationCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (lc *LocationCategory) Unwrap() *LocationCategory {
	_tx, ok := lc.config.driver.(*txDriver)
	if !ok {
		panic("ent: LocationCategory is not a transactional entity")
	}
	lc.config.driver = _tx.drv
	return lc
}

// String implements the fmt.Stringer.
func (lc *LocationCategory) String() string {
	var builder strings.Builder
	builder.WriteString("LocationCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", lc.ID))
	builder.WriteString("business_unit_id=")
	builder.WriteString(fmt.Sprintf("%v", lc.BusinessUnitID))
	builder.WriteString(", ")
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", lc.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(lc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(lc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(lc.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(lc.Description)
	builder.WriteString(", ")
	builder.WriteString("color=")
	builder.WriteString(lc.Color)
	builder.WriteByte(')')
	return builder.String()
}

// LocationCategories is a parsable slice of LocationCategory.
type LocationCategories []*LocationCategory
