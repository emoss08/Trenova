// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/emoss08/trenova/internal/ent/businessunit"
	"github.com/emoss08/trenova/internal/ent/organization"
	"github.com/emoss08/trenova/internal/ent/user"
	"github.com/emoss08/trenova/internal/ent/userreport"
	"github.com/google/uuid"
)

// UserReport is the model entity for the UserReport schema.
type UserReport struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BusinessUnitID holds the value of the "business_unit_id" field.
	BusinessUnitID uuid.UUID `json:"businessUnitId"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organizationId"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt" validate:"omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updatedAt" validate:"omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version" validate:"omitempty"`
	// ReportURL holds the value of the "report_url" field.
	ReportURL string `json:"reportUrl" validate:"required"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"userId" validate:"required,uuid"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserReportQuery when eager-loading is set.
	Edges        UserReportEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserReportEdges holds the relations/edges for other nodes in the graph.
type UserReportEdges struct {
	// BusinessUnit holds the value of the business_unit edge.
	BusinessUnit *BusinessUnit `json:"business_unit,omitempty"`
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// BusinessUnitOrErr returns the BusinessUnit value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserReportEdges) BusinessUnitOrErr() (*BusinessUnit, error) {
	if e.BusinessUnit != nil {
		return e.BusinessUnit, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: businessunit.Label}
	}
	return nil, &NotLoadedError{edge: "business_unit"}
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserReportEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserReportEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserReport) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userreport.FieldVersion:
			values[i] = new(sql.NullInt64)
		case userreport.FieldReportURL:
			values[i] = new(sql.NullString)
		case userreport.FieldCreatedAt, userreport.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case userreport.FieldID, userreport.FieldBusinessUnitID, userreport.FieldOrganizationID, userreport.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserReport fields.
func (ur *UserReport) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userreport.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ur.ID = *value
			}
		case userreport.FieldBusinessUnitID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field business_unit_id", values[i])
			} else if value != nil {
				ur.BusinessUnitID = *value
			}
		case userreport.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				ur.OrganizationID = *value
			}
		case userreport.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ur.CreatedAt = value.Time
			}
		case userreport.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ur.UpdatedAt = value.Time
			}
		case userreport.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				ur.Version = int(value.Int64)
			}
		case userreport.FieldReportURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field report_url", values[i])
			} else if value.Valid {
				ur.ReportURL = value.String
			}
		case userreport.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				ur.UserID = *value
			}
		default:
			ur.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserReport.
// This includes values selected through modifiers, order, etc.
func (ur *UserReport) Value(name string) (ent.Value, error) {
	return ur.selectValues.Get(name)
}

// QueryBusinessUnit queries the "business_unit" edge of the UserReport entity.
func (ur *UserReport) QueryBusinessUnit() *BusinessUnitQuery {
	return NewUserReportClient(ur.config).QueryBusinessUnit(ur)
}

// QueryOrganization queries the "organization" edge of the UserReport entity.
func (ur *UserReport) QueryOrganization() *OrganizationQuery {
	return NewUserReportClient(ur.config).QueryOrganization(ur)
}

// QueryUser queries the "user" edge of the UserReport entity.
func (ur *UserReport) QueryUser() *UserQuery {
	return NewUserReportClient(ur.config).QueryUser(ur)
}

// Update returns a builder for updating this UserReport.
// Note that you need to call UserReport.Unwrap() before calling this method if this UserReport
// was returned from a transaction, and the transaction was committed or rolled back.
func (ur *UserReport) Update() *UserReportUpdateOne {
	return NewUserReportClient(ur.config).UpdateOne(ur)
}

// Unwrap unwraps the UserReport entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ur *UserReport) Unwrap() *UserReport {
	_tx, ok := ur.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserReport is not a transactional entity")
	}
	ur.config.driver = _tx.drv
	return ur
}

// String implements the fmt.Stringer.
func (ur *UserReport) String() string {
	var builder strings.Builder
	builder.WriteString("UserReport(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ur.ID))
	builder.WriteString("business_unit_id=")
	builder.WriteString(fmt.Sprintf("%v", ur.BusinessUnitID))
	builder.WriteString(", ")
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", ur.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ur.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ur.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", ur.Version))
	builder.WriteString(", ")
	builder.WriteString("report_url=")
	builder.WriteString(ur.ReportURL)
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ur.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// UserReports is a parsable slice of UserReport.
type UserReports []*UserReport
