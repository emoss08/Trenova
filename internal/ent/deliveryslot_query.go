// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/internal/ent/businessunit"
	"github.com/emoss08/trenova/internal/ent/customer"
	"github.com/emoss08/trenova/internal/ent/deliveryslot"
	"github.com/emoss08/trenova/internal/ent/location"
	"github.com/emoss08/trenova/internal/ent/organization"
	"github.com/emoss08/trenova/internal/ent/predicate"
	"github.com/google/uuid"
)

// DeliverySlotQuery is the builder for querying DeliverySlot entities.
type DeliverySlotQuery struct {
	config
	ctx              *QueryContext
	order            []deliveryslot.OrderOption
	inters           []Interceptor
	predicates       []predicate.DeliverySlot
	withBusinessUnit *BusinessUnitQuery
	withOrganization *OrganizationQuery
	withCustomer     *CustomerQuery
	withLocation     *LocationQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeliverySlotQuery builder.
func (dsq *DeliverySlotQuery) Where(ps ...predicate.DeliverySlot) *DeliverySlotQuery {
	dsq.predicates = append(dsq.predicates, ps...)
	return dsq
}

// Limit the number of records to be returned by this query.
func (dsq *DeliverySlotQuery) Limit(limit int) *DeliverySlotQuery {
	dsq.ctx.Limit = &limit
	return dsq
}

// Offset to start from.
func (dsq *DeliverySlotQuery) Offset(offset int) *DeliverySlotQuery {
	dsq.ctx.Offset = &offset
	return dsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dsq *DeliverySlotQuery) Unique(unique bool) *DeliverySlotQuery {
	dsq.ctx.Unique = &unique
	return dsq
}

// Order specifies how the records should be ordered.
func (dsq *DeliverySlotQuery) Order(o ...deliveryslot.OrderOption) *DeliverySlotQuery {
	dsq.order = append(dsq.order, o...)
	return dsq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (dsq *DeliverySlotQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: dsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deliveryslot.Table, deliveryslot.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, deliveryslot.BusinessUnitTable, deliveryslot.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(dsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (dsq *DeliverySlotQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: dsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deliveryslot.Table, deliveryslot.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, deliveryslot.OrganizationTable, deliveryslot.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(dsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCustomer chains the current query on the "customer" edge.
func (dsq *DeliverySlotQuery) QueryCustomer() *CustomerQuery {
	query := (&CustomerClient{config: dsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deliveryslot.Table, deliveryslot.FieldID, selector),
			sqlgraph.To(customer.Table, customer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deliveryslot.CustomerTable, deliveryslot.CustomerColumn),
		)
		fromU = sqlgraph.SetNeighbors(dsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLocation chains the current query on the "location" edge.
func (dsq *DeliverySlotQuery) QueryLocation() *LocationQuery {
	query := (&LocationClient{config: dsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deliveryslot.Table, deliveryslot.FieldID, selector),
			sqlgraph.To(location.Table, location.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, deliveryslot.LocationTable, deliveryslot.LocationColumn),
		)
		fromU = sqlgraph.SetNeighbors(dsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DeliverySlot entity from the query.
// Returns a *NotFoundError when no DeliverySlot was found.
func (dsq *DeliverySlotQuery) First(ctx context.Context) (*DeliverySlot, error) {
	nodes, err := dsq.Limit(1).All(setContextOp(ctx, dsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deliveryslot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dsq *DeliverySlotQuery) FirstX(ctx context.Context) *DeliverySlot {
	node, err := dsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeliverySlot ID from the query.
// Returns a *NotFoundError when no DeliverySlot ID was found.
func (dsq *DeliverySlotQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dsq.Limit(1).IDs(setContextOp(ctx, dsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deliveryslot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dsq *DeliverySlotQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := dsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeliverySlot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeliverySlot entity is found.
// Returns a *NotFoundError when no DeliverySlot entities are found.
func (dsq *DeliverySlotQuery) Only(ctx context.Context) (*DeliverySlot, error) {
	nodes, err := dsq.Limit(2).All(setContextOp(ctx, dsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deliveryslot.Label}
	default:
		return nil, &NotSingularError{deliveryslot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dsq *DeliverySlotQuery) OnlyX(ctx context.Context) *DeliverySlot {
	node, err := dsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeliverySlot ID in the query.
// Returns a *NotSingularError when more than one DeliverySlot ID is found.
// Returns a *NotFoundError when no entities are found.
func (dsq *DeliverySlotQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dsq.Limit(2).IDs(setContextOp(ctx, dsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deliveryslot.Label}
	default:
		err = &NotSingularError{deliveryslot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dsq *DeliverySlotQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := dsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeliverySlots.
func (dsq *DeliverySlotQuery) All(ctx context.Context) ([]*DeliverySlot, error) {
	ctx = setContextOp(ctx, dsq.ctx, "All")
	if err := dsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeliverySlot, *DeliverySlotQuery]()
	return withInterceptors[[]*DeliverySlot](ctx, dsq, qr, dsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dsq *DeliverySlotQuery) AllX(ctx context.Context) []*DeliverySlot {
	nodes, err := dsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeliverySlot IDs.
func (dsq *DeliverySlotQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if dsq.ctx.Unique == nil && dsq.path != nil {
		dsq.Unique(true)
	}
	ctx = setContextOp(ctx, dsq.ctx, "IDs")
	if err = dsq.Select(deliveryslot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dsq *DeliverySlotQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := dsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dsq *DeliverySlotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dsq.ctx, "Count")
	if err := dsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dsq, querierCount[*DeliverySlotQuery](), dsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dsq *DeliverySlotQuery) CountX(ctx context.Context) int {
	count, err := dsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dsq *DeliverySlotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dsq.ctx, "Exist")
	switch _, err := dsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dsq *DeliverySlotQuery) ExistX(ctx context.Context) bool {
	exist, err := dsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeliverySlotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dsq *DeliverySlotQuery) Clone() *DeliverySlotQuery {
	if dsq == nil {
		return nil
	}
	return &DeliverySlotQuery{
		config:           dsq.config,
		ctx:              dsq.ctx.Clone(),
		order:            append([]deliveryslot.OrderOption{}, dsq.order...),
		inters:           append([]Interceptor{}, dsq.inters...),
		predicates:       append([]predicate.DeliverySlot{}, dsq.predicates...),
		withBusinessUnit: dsq.withBusinessUnit.Clone(),
		withOrganization: dsq.withOrganization.Clone(),
		withCustomer:     dsq.withCustomer.Clone(),
		withLocation:     dsq.withLocation.Clone(),
		// clone intermediate query.
		sql:  dsq.sql.Clone(),
		path: dsq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (dsq *DeliverySlotQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *DeliverySlotQuery {
	query := (&BusinessUnitClient{config: dsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dsq.withBusinessUnit = query
	return dsq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (dsq *DeliverySlotQuery) WithOrganization(opts ...func(*OrganizationQuery)) *DeliverySlotQuery {
	query := (&OrganizationClient{config: dsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dsq.withOrganization = query
	return dsq
}

// WithCustomer tells the query-builder to eager-load the nodes that are connected to
// the "customer" edge. The optional arguments are used to configure the query builder of the edge.
func (dsq *DeliverySlotQuery) WithCustomer(opts ...func(*CustomerQuery)) *DeliverySlotQuery {
	query := (&CustomerClient{config: dsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dsq.withCustomer = query
	return dsq
}

// WithLocation tells the query-builder to eager-load the nodes that are connected to
// the "location" edge. The optional arguments are used to configure the query builder of the edge.
func (dsq *DeliverySlotQuery) WithLocation(opts ...func(*LocationQuery)) *DeliverySlotQuery {
	query := (&LocationClient{config: dsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dsq.withLocation = query
	return dsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeliverySlot.Query().
//		GroupBy(deliveryslot.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dsq *DeliverySlotQuery) GroupBy(field string, fields ...string) *DeliverySlotGroupBy {
	dsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeliverySlotGroupBy{build: dsq}
	grbuild.flds = &dsq.ctx.Fields
	grbuild.label = deliveryslot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.DeliverySlot.Query().
//		Select(deliveryslot.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (dsq *DeliverySlotQuery) Select(fields ...string) *DeliverySlotSelect {
	dsq.ctx.Fields = append(dsq.ctx.Fields, fields...)
	sbuild := &DeliverySlotSelect{DeliverySlotQuery: dsq}
	sbuild.label = deliveryslot.Label
	sbuild.flds, sbuild.scan = &dsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeliverySlotSelect configured with the given aggregations.
func (dsq *DeliverySlotQuery) Aggregate(fns ...AggregateFunc) *DeliverySlotSelect {
	return dsq.Select().Aggregate(fns...)
}

func (dsq *DeliverySlotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dsq); err != nil {
				return err
			}
		}
	}
	for _, f := range dsq.ctx.Fields {
		if !deliveryslot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dsq.path != nil {
		prev, err := dsq.path(ctx)
		if err != nil {
			return err
		}
		dsq.sql = prev
	}
	return nil
}

func (dsq *DeliverySlotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeliverySlot, error) {
	var (
		nodes       = []*DeliverySlot{}
		_spec       = dsq.querySpec()
		loadedTypes = [4]bool{
			dsq.withBusinessUnit != nil,
			dsq.withOrganization != nil,
			dsq.withCustomer != nil,
			dsq.withLocation != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeliverySlot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeliverySlot{config: dsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(dsq.modifiers) > 0 {
		_spec.Modifiers = dsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dsq.withBusinessUnit; query != nil {
		if err := dsq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *DeliverySlot, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := dsq.withOrganization; query != nil {
		if err := dsq.loadOrganization(ctx, query, nodes, nil,
			func(n *DeliverySlot, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := dsq.withCustomer; query != nil {
		if err := dsq.loadCustomer(ctx, query, nodes, nil,
			func(n *DeliverySlot, e *Customer) { n.Edges.Customer = e }); err != nil {
			return nil, err
		}
	}
	if query := dsq.withLocation; query != nil {
		if err := dsq.loadLocation(ctx, query, nodes, nil,
			func(n *DeliverySlot, e *Location) { n.Edges.Location = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dsq *DeliverySlotQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*DeliverySlot, init func(*DeliverySlot), assign func(*DeliverySlot, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DeliverySlot)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dsq *DeliverySlotQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*DeliverySlot, init func(*DeliverySlot), assign func(*DeliverySlot, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DeliverySlot)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dsq *DeliverySlotQuery) loadCustomer(ctx context.Context, query *CustomerQuery, nodes []*DeliverySlot, init func(*DeliverySlot), assign func(*DeliverySlot, *Customer)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DeliverySlot)
	for i := range nodes {
		fk := nodes[i].CustomerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(customer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "customer_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dsq *DeliverySlotQuery) loadLocation(ctx context.Context, query *LocationQuery, nodes []*DeliverySlot, init func(*DeliverySlot), assign func(*DeliverySlot, *Location)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DeliverySlot)
	for i := range nodes {
		fk := nodes[i].LocationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(location.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "location_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dsq *DeliverySlotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dsq.querySpec()
	if len(dsq.modifiers) > 0 {
		_spec.Modifiers = dsq.modifiers
	}
	_spec.Node.Columns = dsq.ctx.Fields
	if len(dsq.ctx.Fields) > 0 {
		_spec.Unique = dsq.ctx.Unique != nil && *dsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dsq.driver, _spec)
}

func (dsq *DeliverySlotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deliveryslot.Table, deliveryslot.Columns, sqlgraph.NewFieldSpec(deliveryslot.FieldID, field.TypeUUID))
	_spec.From = dsq.sql
	if unique := dsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dsq.path != nil {
		_spec.Unique = true
	}
	if fields := dsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deliveryslot.FieldID)
		for i := range fields {
			if fields[i] != deliveryslot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dsq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(deliveryslot.FieldBusinessUnitID)
		}
		if dsq.withOrganization != nil {
			_spec.Node.AddColumnOnce(deliveryslot.FieldOrganizationID)
		}
		if dsq.withCustomer != nil {
			_spec.Node.AddColumnOnce(deliveryslot.FieldCustomerID)
		}
		if dsq.withLocation != nil {
			_spec.Node.AddColumnOnce(deliveryslot.FieldLocationID)
		}
	}
	if ps := dsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dsq *DeliverySlotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dsq.driver.Dialect())
	t1 := builder.Table(deliveryslot.Table)
	columns := dsq.ctx.Fields
	if len(columns) == 0 {
		columns = deliveryslot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dsq.sql != nil {
		selector = dsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dsq.ctx.Unique != nil && *dsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range dsq.modifiers {
		m(selector)
	}
	for _, p := range dsq.predicates {
		p(selector)
	}
	for _, p := range dsq.order {
		p(selector)
	}
	if offset := dsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dsq *DeliverySlotQuery) Modify(modifiers ...func(s *sql.Selector)) *DeliverySlotSelect {
	dsq.modifiers = append(dsq.modifiers, modifiers...)
	return dsq.Select()
}

// DeliverySlotGroupBy is the group-by builder for DeliverySlot entities.
type DeliverySlotGroupBy struct {
	selector
	build *DeliverySlotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dsgb *DeliverySlotGroupBy) Aggregate(fns ...AggregateFunc) *DeliverySlotGroupBy {
	dsgb.fns = append(dsgb.fns, fns...)
	return dsgb
}

// Scan applies the selector query and scans the result into the given value.
func (dsgb *DeliverySlotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dsgb.build.ctx, "GroupBy")
	if err := dsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeliverySlotQuery, *DeliverySlotGroupBy](ctx, dsgb.build, dsgb, dsgb.build.inters, v)
}

func (dsgb *DeliverySlotGroupBy) sqlScan(ctx context.Context, root *DeliverySlotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dsgb.fns))
	for _, fn := range dsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dsgb.flds)+len(dsgb.fns))
		for _, f := range *dsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeliverySlotSelect is the builder for selecting fields of DeliverySlot entities.
type DeliverySlotSelect struct {
	*DeliverySlotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dss *DeliverySlotSelect) Aggregate(fns ...AggregateFunc) *DeliverySlotSelect {
	dss.fns = append(dss.fns, fns...)
	return dss
}

// Scan applies the selector query and scans the result into the given value.
func (dss *DeliverySlotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dss.ctx, "Select")
	if err := dss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeliverySlotQuery, *DeliverySlotSelect](ctx, dss.DeliverySlotQuery, dss, dss.inters, v)
}

func (dss *DeliverySlotSelect) sqlScan(ctx context.Context, root *DeliverySlotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dss.fns))
	for _, fn := range dss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dss *DeliverySlotSelect) Modify(modifiers ...func(s *sql.Selector)) *DeliverySlotSelect {
	dss.modifiers = append(dss.modifiers, modifiers...)
	return dss
}
