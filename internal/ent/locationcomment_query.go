// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/internal/ent/businessunit"
	"github.com/emoss08/trenova/internal/ent/commenttype"
	"github.com/emoss08/trenova/internal/ent/location"
	"github.com/emoss08/trenova/internal/ent/locationcomment"
	"github.com/emoss08/trenova/internal/ent/organization"
	"github.com/emoss08/trenova/internal/ent/predicate"
	"github.com/emoss08/trenova/internal/ent/user"
	"github.com/google/uuid"
)

// LocationCommentQuery is the builder for querying LocationComment entities.
type LocationCommentQuery struct {
	config
	ctx              *QueryContext
	order            []locationcomment.OrderOption
	inters           []Interceptor
	predicates       []predicate.LocationComment
	withBusinessUnit *BusinessUnitQuery
	withOrganization *OrganizationQuery
	withLocation     *LocationQuery
	withUser         *UserQuery
	withCommentType  *CommentTypeQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LocationCommentQuery builder.
func (lcq *LocationCommentQuery) Where(ps ...predicate.LocationComment) *LocationCommentQuery {
	lcq.predicates = append(lcq.predicates, ps...)
	return lcq
}

// Limit the number of records to be returned by this query.
func (lcq *LocationCommentQuery) Limit(limit int) *LocationCommentQuery {
	lcq.ctx.Limit = &limit
	return lcq
}

// Offset to start from.
func (lcq *LocationCommentQuery) Offset(offset int) *LocationCommentQuery {
	lcq.ctx.Offset = &offset
	return lcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lcq *LocationCommentQuery) Unique(unique bool) *LocationCommentQuery {
	lcq.ctx.Unique = &unique
	return lcq
}

// Order specifies how the records should be ordered.
func (lcq *LocationCommentQuery) Order(o ...locationcomment.OrderOption) *LocationCommentQuery {
	lcq.order = append(lcq.order, o...)
	return lcq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (lcq *LocationCommentQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(locationcomment.Table, locationcomment.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, locationcomment.BusinessUnitTable, locationcomment.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (lcq *LocationCommentQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(locationcomment.Table, locationcomment.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, locationcomment.OrganizationTable, locationcomment.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLocation chains the current query on the "location" edge.
func (lcq *LocationCommentQuery) QueryLocation() *LocationQuery {
	query := (&LocationClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(locationcomment.Table, locationcomment.FieldID, selector),
			sqlgraph.To(location.Table, location.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, locationcomment.LocationTable, locationcomment.LocationColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (lcq *LocationCommentQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(locationcomment.Table, locationcomment.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, locationcomment.UserTable, locationcomment.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCommentType chains the current query on the "comment_type" edge.
func (lcq *LocationCommentQuery) QueryCommentType() *CommentTypeQuery {
	query := (&CommentTypeClient{config: lcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(locationcomment.Table, locationcomment.FieldID, selector),
			sqlgraph.To(commenttype.Table, commenttype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, locationcomment.CommentTypeTable, locationcomment.CommentTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(lcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LocationComment entity from the query.
// Returns a *NotFoundError when no LocationComment was found.
func (lcq *LocationCommentQuery) First(ctx context.Context) (*LocationComment, error) {
	nodes, err := lcq.Limit(1).All(setContextOp(ctx, lcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{locationcomment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lcq *LocationCommentQuery) FirstX(ctx context.Context) *LocationComment {
	node, err := lcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LocationComment ID from the query.
// Returns a *NotFoundError when no LocationComment ID was found.
func (lcq *LocationCommentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lcq.Limit(1).IDs(setContextOp(ctx, lcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{locationcomment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lcq *LocationCommentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := lcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LocationComment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LocationComment entity is found.
// Returns a *NotFoundError when no LocationComment entities are found.
func (lcq *LocationCommentQuery) Only(ctx context.Context) (*LocationComment, error) {
	nodes, err := lcq.Limit(2).All(setContextOp(ctx, lcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{locationcomment.Label}
	default:
		return nil, &NotSingularError{locationcomment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lcq *LocationCommentQuery) OnlyX(ctx context.Context) *LocationComment {
	node, err := lcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LocationComment ID in the query.
// Returns a *NotSingularError when more than one LocationComment ID is found.
// Returns a *NotFoundError when no entities are found.
func (lcq *LocationCommentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lcq.Limit(2).IDs(setContextOp(ctx, lcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{locationcomment.Label}
	default:
		err = &NotSingularError{locationcomment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lcq *LocationCommentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := lcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LocationComments.
func (lcq *LocationCommentQuery) All(ctx context.Context) ([]*LocationComment, error) {
	ctx = setContextOp(ctx, lcq.ctx, "All")
	if err := lcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LocationComment, *LocationCommentQuery]()
	return withInterceptors[[]*LocationComment](ctx, lcq, qr, lcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lcq *LocationCommentQuery) AllX(ctx context.Context) []*LocationComment {
	nodes, err := lcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LocationComment IDs.
func (lcq *LocationCommentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if lcq.ctx.Unique == nil && lcq.path != nil {
		lcq.Unique(true)
	}
	ctx = setContextOp(ctx, lcq.ctx, "IDs")
	if err = lcq.Select(locationcomment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lcq *LocationCommentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := lcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lcq *LocationCommentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lcq.ctx, "Count")
	if err := lcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lcq, querierCount[*LocationCommentQuery](), lcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lcq *LocationCommentQuery) CountX(ctx context.Context) int {
	count, err := lcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lcq *LocationCommentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lcq.ctx, "Exist")
	switch _, err := lcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lcq *LocationCommentQuery) ExistX(ctx context.Context) bool {
	exist, err := lcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LocationCommentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lcq *LocationCommentQuery) Clone() *LocationCommentQuery {
	if lcq == nil {
		return nil
	}
	return &LocationCommentQuery{
		config:           lcq.config,
		ctx:              lcq.ctx.Clone(),
		order:            append([]locationcomment.OrderOption{}, lcq.order...),
		inters:           append([]Interceptor{}, lcq.inters...),
		predicates:       append([]predicate.LocationComment{}, lcq.predicates...),
		withBusinessUnit: lcq.withBusinessUnit.Clone(),
		withOrganization: lcq.withOrganization.Clone(),
		withLocation:     lcq.withLocation.Clone(),
		withUser:         lcq.withUser.Clone(),
		withCommentType:  lcq.withCommentType.Clone(),
		// clone intermediate query.
		sql:  lcq.sql.Clone(),
		path: lcq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LocationCommentQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *LocationCommentQuery {
	query := (&BusinessUnitClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withBusinessUnit = query
	return lcq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LocationCommentQuery) WithOrganization(opts ...func(*OrganizationQuery)) *LocationCommentQuery {
	query := (&OrganizationClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withOrganization = query
	return lcq
}

// WithLocation tells the query-builder to eager-load the nodes that are connected to
// the "location" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LocationCommentQuery) WithLocation(opts ...func(*LocationQuery)) *LocationCommentQuery {
	query := (&LocationClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withLocation = query
	return lcq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LocationCommentQuery) WithUser(opts ...func(*UserQuery)) *LocationCommentQuery {
	query := (&UserClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withUser = query
	return lcq
}

// WithCommentType tells the query-builder to eager-load the nodes that are connected to
// the "comment_type" edge. The optional arguments are used to configure the query builder of the edge.
func (lcq *LocationCommentQuery) WithCommentType(opts ...func(*CommentTypeQuery)) *LocationCommentQuery {
	query := (&CommentTypeClient{config: lcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lcq.withCommentType = query
	return lcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LocationComment.Query().
//		GroupBy(locationcomment.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lcq *LocationCommentQuery) GroupBy(field string, fields ...string) *LocationCommentGroupBy {
	lcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LocationCommentGroupBy{build: lcq}
	grbuild.flds = &lcq.ctx.Fields
	grbuild.label = locationcomment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.LocationComment.Query().
//		Select(locationcomment.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (lcq *LocationCommentQuery) Select(fields ...string) *LocationCommentSelect {
	lcq.ctx.Fields = append(lcq.ctx.Fields, fields...)
	sbuild := &LocationCommentSelect{LocationCommentQuery: lcq}
	sbuild.label = locationcomment.Label
	sbuild.flds, sbuild.scan = &lcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LocationCommentSelect configured with the given aggregations.
func (lcq *LocationCommentQuery) Aggregate(fns ...AggregateFunc) *LocationCommentSelect {
	return lcq.Select().Aggregate(fns...)
}

func (lcq *LocationCommentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lcq); err != nil {
				return err
			}
		}
	}
	for _, f := range lcq.ctx.Fields {
		if !locationcomment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lcq.path != nil {
		prev, err := lcq.path(ctx)
		if err != nil {
			return err
		}
		lcq.sql = prev
	}
	return nil
}

func (lcq *LocationCommentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LocationComment, error) {
	var (
		nodes       = []*LocationComment{}
		_spec       = lcq.querySpec()
		loadedTypes = [5]bool{
			lcq.withBusinessUnit != nil,
			lcq.withOrganization != nil,
			lcq.withLocation != nil,
			lcq.withUser != nil,
			lcq.withCommentType != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LocationComment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LocationComment{config: lcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(lcq.modifiers) > 0 {
		_spec.Modifiers = lcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lcq.withBusinessUnit; query != nil {
		if err := lcq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *LocationComment, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := lcq.withOrganization; query != nil {
		if err := lcq.loadOrganization(ctx, query, nodes, nil,
			func(n *LocationComment, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := lcq.withLocation; query != nil {
		if err := lcq.loadLocation(ctx, query, nodes, nil,
			func(n *LocationComment, e *Location) { n.Edges.Location = e }); err != nil {
			return nil, err
		}
	}
	if query := lcq.withUser; query != nil {
		if err := lcq.loadUser(ctx, query, nodes, nil,
			func(n *LocationComment, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := lcq.withCommentType; query != nil {
		if err := lcq.loadCommentType(ctx, query, nodes, nil,
			func(n *LocationComment, e *CommentType) { n.Edges.CommentType = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lcq *LocationCommentQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*LocationComment, init func(*LocationComment), assign func(*LocationComment, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocationComment)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (lcq *LocationCommentQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*LocationComment, init func(*LocationComment), assign func(*LocationComment, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocationComment)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (lcq *LocationCommentQuery) loadLocation(ctx context.Context, query *LocationQuery, nodes []*LocationComment, init func(*LocationComment), assign func(*LocationComment, *Location)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocationComment)
	for i := range nodes {
		fk := nodes[i].LocationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(location.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "location_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (lcq *LocationCommentQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LocationComment, init func(*LocationComment), assign func(*LocationComment, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocationComment)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (lcq *LocationCommentQuery) loadCommentType(ctx context.Context, query *CommentTypeQuery, nodes []*LocationComment, init func(*LocationComment), assign func(*LocationComment, *CommentType)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocationComment)
	for i := range nodes {
		fk := nodes[i].CommentTypeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(commenttype.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "comment_type_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lcq *LocationCommentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lcq.querySpec()
	if len(lcq.modifiers) > 0 {
		_spec.Modifiers = lcq.modifiers
	}
	_spec.Node.Columns = lcq.ctx.Fields
	if len(lcq.ctx.Fields) > 0 {
		_spec.Unique = lcq.ctx.Unique != nil && *lcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lcq.driver, _spec)
}

func (lcq *LocationCommentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(locationcomment.Table, locationcomment.Columns, sqlgraph.NewFieldSpec(locationcomment.FieldID, field.TypeUUID))
	_spec.From = lcq.sql
	if unique := lcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lcq.path != nil {
		_spec.Unique = true
	}
	if fields := lcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, locationcomment.FieldID)
		for i := range fields {
			if fields[i] != locationcomment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if lcq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(locationcomment.FieldBusinessUnitID)
		}
		if lcq.withOrganization != nil {
			_spec.Node.AddColumnOnce(locationcomment.FieldOrganizationID)
		}
		if lcq.withLocation != nil {
			_spec.Node.AddColumnOnce(locationcomment.FieldLocationID)
		}
		if lcq.withUser != nil {
			_spec.Node.AddColumnOnce(locationcomment.FieldUserID)
		}
		if lcq.withCommentType != nil {
			_spec.Node.AddColumnOnce(locationcomment.FieldCommentTypeID)
		}
	}
	if ps := lcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lcq *LocationCommentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lcq.driver.Dialect())
	t1 := builder.Table(locationcomment.Table)
	columns := lcq.ctx.Fields
	if len(columns) == 0 {
		columns = locationcomment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lcq.sql != nil {
		selector = lcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lcq.ctx.Unique != nil && *lcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range lcq.modifiers {
		m(selector)
	}
	for _, p := range lcq.predicates {
		p(selector)
	}
	for _, p := range lcq.order {
		p(selector)
	}
	if offset := lcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (lcq *LocationCommentQuery) Modify(modifiers ...func(s *sql.Selector)) *LocationCommentSelect {
	lcq.modifiers = append(lcq.modifiers, modifiers...)
	return lcq.Select()
}

// LocationCommentGroupBy is the group-by builder for LocationComment entities.
type LocationCommentGroupBy struct {
	selector
	build *LocationCommentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lcgb *LocationCommentGroupBy) Aggregate(fns ...AggregateFunc) *LocationCommentGroupBy {
	lcgb.fns = append(lcgb.fns, fns...)
	return lcgb
}

// Scan applies the selector query and scans the result into the given value.
func (lcgb *LocationCommentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcgb.build.ctx, "GroupBy")
	if err := lcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LocationCommentQuery, *LocationCommentGroupBy](ctx, lcgb.build, lcgb, lcgb.build.inters, v)
}

func (lcgb *LocationCommentGroupBy) sqlScan(ctx context.Context, root *LocationCommentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lcgb.fns))
	for _, fn := range lcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lcgb.flds)+len(lcgb.fns))
		for _, f := range *lcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LocationCommentSelect is the builder for selecting fields of LocationComment entities.
type LocationCommentSelect struct {
	*LocationCommentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lcs *LocationCommentSelect) Aggregate(fns ...AggregateFunc) *LocationCommentSelect {
	lcs.fns = append(lcs.fns, fns...)
	return lcs
}

// Scan applies the selector query and scans the result into the given value.
func (lcs *LocationCommentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcs.ctx, "Select")
	if err := lcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LocationCommentQuery, *LocationCommentSelect](ctx, lcs.LocationCommentQuery, lcs, lcs.inters, v)
}

func (lcs *LocationCommentSelect) sqlScan(ctx context.Context, root *LocationCommentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lcs.fns))
	for _, fn := range lcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (lcs *LocationCommentSelect) Modify(modifiers ...func(s *sql.Selector)) *LocationCommentSelect {
	lcs.modifiers = append(lcs.modifiers, modifiers...)
	return lcs
}
