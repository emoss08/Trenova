// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/emoss08/trenova/internal/ent/businessunit"
	"github.com/emoss08/trenova/internal/ent/hazardousmaterialsegregation"
	"github.com/emoss08/trenova/internal/ent/organization"
	"github.com/emoss08/trenova/internal/ent/predicate"
	"github.com/google/uuid"
)

// HazardousMaterialSegregationQuery is the builder for querying HazardousMaterialSegregation entities.
type HazardousMaterialSegregationQuery struct {
	config
	ctx              *QueryContext
	order            []hazardousmaterialsegregation.OrderOption
	inters           []Interceptor
	predicates       []predicate.HazardousMaterialSegregation
	withBusinessUnit *BusinessUnitQuery
	withOrganization *OrganizationQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HazardousMaterialSegregationQuery builder.
func (hmsq *HazardousMaterialSegregationQuery) Where(ps ...predicate.HazardousMaterialSegregation) *HazardousMaterialSegregationQuery {
	hmsq.predicates = append(hmsq.predicates, ps...)
	return hmsq
}

// Limit the number of records to be returned by this query.
func (hmsq *HazardousMaterialSegregationQuery) Limit(limit int) *HazardousMaterialSegregationQuery {
	hmsq.ctx.Limit = &limit
	return hmsq
}

// Offset to start from.
func (hmsq *HazardousMaterialSegregationQuery) Offset(offset int) *HazardousMaterialSegregationQuery {
	hmsq.ctx.Offset = &offset
	return hmsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hmsq *HazardousMaterialSegregationQuery) Unique(unique bool) *HazardousMaterialSegregationQuery {
	hmsq.ctx.Unique = &unique
	return hmsq
}

// Order specifies how the records should be ordered.
func (hmsq *HazardousMaterialSegregationQuery) Order(o ...hazardousmaterialsegregation.OrderOption) *HazardousMaterialSegregationQuery {
	hmsq.order = append(hmsq.order, o...)
	return hmsq
}

// QueryBusinessUnit chains the current query on the "business_unit" edge.
func (hmsq *HazardousMaterialSegregationQuery) QueryBusinessUnit() *BusinessUnitQuery {
	query := (&BusinessUnitClient{config: hmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hazardousmaterialsegregation.Table, hazardousmaterialsegregation.FieldID, selector),
			sqlgraph.To(businessunit.Table, businessunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, hazardousmaterialsegregation.BusinessUnitTable, hazardousmaterialsegregation.BusinessUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(hmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (hmsq *HazardousMaterialSegregationQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: hmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hazardousmaterialsegregation.Table, hazardousmaterialsegregation.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, hazardousmaterialsegregation.OrganizationTable, hazardousmaterialsegregation.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(hmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first HazardousMaterialSegregation entity from the query.
// Returns a *NotFoundError when no HazardousMaterialSegregation was found.
func (hmsq *HazardousMaterialSegregationQuery) First(ctx context.Context) (*HazardousMaterialSegregation, error) {
	nodes, err := hmsq.Limit(1).All(setContextOp(ctx, hmsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hazardousmaterialsegregation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) FirstX(ctx context.Context) *HazardousMaterialSegregation {
	node, err := hmsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HazardousMaterialSegregation ID from the query.
// Returns a *NotFoundError when no HazardousMaterialSegregation ID was found.
func (hmsq *HazardousMaterialSegregationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hmsq.Limit(1).IDs(setContextOp(ctx, hmsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hazardousmaterialsegregation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := hmsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HazardousMaterialSegregation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HazardousMaterialSegregation entity is found.
// Returns a *NotFoundError when no HazardousMaterialSegregation entities are found.
func (hmsq *HazardousMaterialSegregationQuery) Only(ctx context.Context) (*HazardousMaterialSegregation, error) {
	nodes, err := hmsq.Limit(2).All(setContextOp(ctx, hmsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hazardousmaterialsegregation.Label}
	default:
		return nil, &NotSingularError{hazardousmaterialsegregation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) OnlyX(ctx context.Context) *HazardousMaterialSegregation {
	node, err := hmsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HazardousMaterialSegregation ID in the query.
// Returns a *NotSingularError when more than one HazardousMaterialSegregation ID is found.
// Returns a *NotFoundError when no entities are found.
func (hmsq *HazardousMaterialSegregationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hmsq.Limit(2).IDs(setContextOp(ctx, hmsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hazardousmaterialsegregation.Label}
	default:
		err = &NotSingularError{hazardousmaterialsegregation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := hmsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HazardousMaterialSegregations.
func (hmsq *HazardousMaterialSegregationQuery) All(ctx context.Context) ([]*HazardousMaterialSegregation, error) {
	ctx = setContextOp(ctx, hmsq.ctx, "All")
	if err := hmsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HazardousMaterialSegregation, *HazardousMaterialSegregationQuery]()
	return withInterceptors[[]*HazardousMaterialSegregation](ctx, hmsq, qr, hmsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) AllX(ctx context.Context) []*HazardousMaterialSegregation {
	nodes, err := hmsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HazardousMaterialSegregation IDs.
func (hmsq *HazardousMaterialSegregationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if hmsq.ctx.Unique == nil && hmsq.path != nil {
		hmsq.Unique(true)
	}
	ctx = setContextOp(ctx, hmsq.ctx, "IDs")
	if err = hmsq.Select(hazardousmaterialsegregation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := hmsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hmsq *HazardousMaterialSegregationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hmsq.ctx, "Count")
	if err := hmsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hmsq, querierCount[*HazardousMaterialSegregationQuery](), hmsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) CountX(ctx context.Context) int {
	count, err := hmsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hmsq *HazardousMaterialSegregationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hmsq.ctx, "Exist")
	switch _, err := hmsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hmsq *HazardousMaterialSegregationQuery) ExistX(ctx context.Context) bool {
	exist, err := hmsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HazardousMaterialSegregationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hmsq *HazardousMaterialSegregationQuery) Clone() *HazardousMaterialSegregationQuery {
	if hmsq == nil {
		return nil
	}
	return &HazardousMaterialSegregationQuery{
		config:           hmsq.config,
		ctx:              hmsq.ctx.Clone(),
		order:            append([]hazardousmaterialsegregation.OrderOption{}, hmsq.order...),
		inters:           append([]Interceptor{}, hmsq.inters...),
		predicates:       append([]predicate.HazardousMaterialSegregation{}, hmsq.predicates...),
		withBusinessUnit: hmsq.withBusinessUnit.Clone(),
		withOrganization: hmsq.withOrganization.Clone(),
		// clone intermediate query.
		sql:  hmsq.sql.Clone(),
		path: hmsq.path,
	}
}

// WithBusinessUnit tells the query-builder to eager-load the nodes that are connected to
// the "business_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (hmsq *HazardousMaterialSegregationQuery) WithBusinessUnit(opts ...func(*BusinessUnitQuery)) *HazardousMaterialSegregationQuery {
	query := (&BusinessUnitClient{config: hmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hmsq.withBusinessUnit = query
	return hmsq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (hmsq *HazardousMaterialSegregationQuery) WithOrganization(opts ...func(*OrganizationQuery)) *HazardousMaterialSegregationQuery {
	query := (&OrganizationClient{config: hmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hmsq.withOrganization = query
	return hmsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HazardousMaterialSegregation.Query().
//		GroupBy(hazardousmaterialsegregation.FieldBusinessUnitID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hmsq *HazardousMaterialSegregationQuery) GroupBy(field string, fields ...string) *HazardousMaterialSegregationGroupBy {
	hmsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HazardousMaterialSegregationGroupBy{build: hmsq}
	grbuild.flds = &hmsq.ctx.Fields
	grbuild.label = hazardousmaterialsegregation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessUnitID uuid.UUID `json:"businessUnitId"`
//	}
//
//	client.HazardousMaterialSegregation.Query().
//		Select(hazardousmaterialsegregation.FieldBusinessUnitID).
//		Scan(ctx, &v)
func (hmsq *HazardousMaterialSegregationQuery) Select(fields ...string) *HazardousMaterialSegregationSelect {
	hmsq.ctx.Fields = append(hmsq.ctx.Fields, fields...)
	sbuild := &HazardousMaterialSegregationSelect{HazardousMaterialSegregationQuery: hmsq}
	sbuild.label = hazardousmaterialsegregation.Label
	sbuild.flds, sbuild.scan = &hmsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HazardousMaterialSegregationSelect configured with the given aggregations.
func (hmsq *HazardousMaterialSegregationQuery) Aggregate(fns ...AggregateFunc) *HazardousMaterialSegregationSelect {
	return hmsq.Select().Aggregate(fns...)
}

func (hmsq *HazardousMaterialSegregationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hmsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hmsq); err != nil {
				return err
			}
		}
	}
	for _, f := range hmsq.ctx.Fields {
		if !hazardousmaterialsegregation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hmsq.path != nil {
		prev, err := hmsq.path(ctx)
		if err != nil {
			return err
		}
		hmsq.sql = prev
	}
	return nil
}

func (hmsq *HazardousMaterialSegregationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HazardousMaterialSegregation, error) {
	var (
		nodes       = []*HazardousMaterialSegregation{}
		_spec       = hmsq.querySpec()
		loadedTypes = [2]bool{
			hmsq.withBusinessUnit != nil,
			hmsq.withOrganization != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HazardousMaterialSegregation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HazardousMaterialSegregation{config: hmsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(hmsq.modifiers) > 0 {
		_spec.Modifiers = hmsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hmsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hmsq.withBusinessUnit; query != nil {
		if err := hmsq.loadBusinessUnit(ctx, query, nodes, nil,
			func(n *HazardousMaterialSegregation, e *BusinessUnit) { n.Edges.BusinessUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := hmsq.withOrganization; query != nil {
		if err := hmsq.loadOrganization(ctx, query, nodes, nil,
			func(n *HazardousMaterialSegregation, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hmsq *HazardousMaterialSegregationQuery) loadBusinessUnit(ctx context.Context, query *BusinessUnitQuery, nodes []*HazardousMaterialSegregation, init func(*HazardousMaterialSegregation), assign func(*HazardousMaterialSegregation, *BusinessUnit)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HazardousMaterialSegregation)
	for i := range nodes {
		fk := nodes[i].BusinessUnitID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(businessunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_unit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hmsq *HazardousMaterialSegregationQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*HazardousMaterialSegregation, init func(*HazardousMaterialSegregation), assign func(*HazardousMaterialSegregation, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HazardousMaterialSegregation)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (hmsq *HazardousMaterialSegregationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hmsq.querySpec()
	if len(hmsq.modifiers) > 0 {
		_spec.Modifiers = hmsq.modifiers
	}
	_spec.Node.Columns = hmsq.ctx.Fields
	if len(hmsq.ctx.Fields) > 0 {
		_spec.Unique = hmsq.ctx.Unique != nil && *hmsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hmsq.driver, _spec)
}

func (hmsq *HazardousMaterialSegregationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hazardousmaterialsegregation.Table, hazardousmaterialsegregation.Columns, sqlgraph.NewFieldSpec(hazardousmaterialsegregation.FieldID, field.TypeUUID))
	_spec.From = hmsq.sql
	if unique := hmsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hmsq.path != nil {
		_spec.Unique = true
	}
	if fields := hmsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hazardousmaterialsegregation.FieldID)
		for i := range fields {
			if fields[i] != hazardousmaterialsegregation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if hmsq.withBusinessUnit != nil {
			_spec.Node.AddColumnOnce(hazardousmaterialsegregation.FieldBusinessUnitID)
		}
		if hmsq.withOrganization != nil {
			_spec.Node.AddColumnOnce(hazardousmaterialsegregation.FieldOrganizationID)
		}
	}
	if ps := hmsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hmsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hmsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hmsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hmsq *HazardousMaterialSegregationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hmsq.driver.Dialect())
	t1 := builder.Table(hazardousmaterialsegregation.Table)
	columns := hmsq.ctx.Fields
	if len(columns) == 0 {
		columns = hazardousmaterialsegregation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hmsq.sql != nil {
		selector = hmsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hmsq.ctx.Unique != nil && *hmsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range hmsq.modifiers {
		m(selector)
	}
	for _, p := range hmsq.predicates {
		p(selector)
	}
	for _, p := range hmsq.order {
		p(selector)
	}
	if offset := hmsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hmsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (hmsq *HazardousMaterialSegregationQuery) Modify(modifiers ...func(s *sql.Selector)) *HazardousMaterialSegregationSelect {
	hmsq.modifiers = append(hmsq.modifiers, modifiers...)
	return hmsq.Select()
}

// HazardousMaterialSegregationGroupBy is the group-by builder for HazardousMaterialSegregation entities.
type HazardousMaterialSegregationGroupBy struct {
	selector
	build *HazardousMaterialSegregationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hmsgb *HazardousMaterialSegregationGroupBy) Aggregate(fns ...AggregateFunc) *HazardousMaterialSegregationGroupBy {
	hmsgb.fns = append(hmsgb.fns, fns...)
	return hmsgb
}

// Scan applies the selector query and scans the result into the given value.
func (hmsgb *HazardousMaterialSegregationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hmsgb.build.ctx, "GroupBy")
	if err := hmsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HazardousMaterialSegregationQuery, *HazardousMaterialSegregationGroupBy](ctx, hmsgb.build, hmsgb, hmsgb.build.inters, v)
}

func (hmsgb *HazardousMaterialSegregationGroupBy) sqlScan(ctx context.Context, root *HazardousMaterialSegregationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hmsgb.fns))
	for _, fn := range hmsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hmsgb.flds)+len(hmsgb.fns))
		for _, f := range *hmsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hmsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hmsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HazardousMaterialSegregationSelect is the builder for selecting fields of HazardousMaterialSegregation entities.
type HazardousMaterialSegregationSelect struct {
	*HazardousMaterialSegregationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hmss *HazardousMaterialSegregationSelect) Aggregate(fns ...AggregateFunc) *HazardousMaterialSegregationSelect {
	hmss.fns = append(hmss.fns, fns...)
	return hmss
}

// Scan applies the selector query and scans the result into the given value.
func (hmss *HazardousMaterialSegregationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hmss.ctx, "Select")
	if err := hmss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HazardousMaterialSegregationQuery, *HazardousMaterialSegregationSelect](ctx, hmss.HazardousMaterialSegregationQuery, hmss, hmss.inters, v)
}

func (hmss *HazardousMaterialSegregationSelect) sqlScan(ctx context.Context, root *HazardousMaterialSegregationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hmss.fns))
	for _, fn := range hmss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hmss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hmss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (hmss *HazardousMaterialSegregationSelect) Modify(modifiers ...func(s *sql.Selector)) *HazardousMaterialSegregationSelect {
	hmss.modifiers = append(hmss.modifiers, modifiers...)
	return hmss
}
