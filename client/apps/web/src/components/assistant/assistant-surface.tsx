/**
 * The layout id the corner mark and the open panel share, so opening the
 * assistant grows the mark into the panel instead of cross-fading two unrelated
 * rectangles.
 */
export const ASSISTANT_SURFACE_ID = "assistant-surface";
