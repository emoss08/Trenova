mutation UpdateSidebarPreferences($input: SidebarPreferencesInput!) {
  updateSidebarPreferences(input: $input) {
    schemaVersion
    version
    sections {
      key
      hidden
    }
    attentionMetrics
    quickActionIds
    activity {
      pageSize
      defaultOpen
    }
  }
}
