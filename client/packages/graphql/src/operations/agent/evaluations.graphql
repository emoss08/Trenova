fragment AgentEvaluationTableRowFields on AgentEvaluation {
  id
  organizationId
  businessUnitId
  agentDefinitionId
  sourceRunId
  evalCaseId
  status
  trigger
  subjectType
  subjectId
  model
  reply
  comparison
  caseScore
  originalProposals
  toolCallsUsed
  errorMessage
  requestedByUserId
  startedAt
  completedAt
  version
  createdAt
  updatedAt
}

query AgentEvaluationTable($input: DataTableConnectionInput!, $includeTotalCount: Boolean = true) {
  agentEvaluations(input: $input) {
    edges {
      node {
        ...AgentEvaluationTableRowFields
      }
    }
    totalCount @include(if: $includeTotalCount)
    pageInfo {
      ...DataTablePageInfoFields
    }
  }
}

query AgentEvaluationDetail($id: ID!) {
  agentEvaluation(id: $id) {
    id
    agentDefinitionId
    sourceRunId
    status
    trigger
    subjectType
    subjectId
    input
    definitionVersion
    promptVersion
    model
    providerId
    reply
    actions
    comparison
    evalCaseId
    checks
    judge
    caseScore
    fingerprint
    originalProposals
    toolCallsUsed
    errorMessage
    startedAt
    completedAt
    createdAt
  }
}

mutation ReplayAgentRun($runId: ID!) {
  replayAgentRun(runId: $runId) {
    ...AgentEvaluationTableRowFields
  }
}
