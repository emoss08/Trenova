//go:build integration

package journalsourcerepository

import (
	"context"
	"testing"
	"time"

	"github.com/emoss08/trenova/internal/core/domain/fiscalperiod"
	"github.com/emoss08/trenova/internal/core/domain/fiscalyear"
	"github.com/emoss08/trenova/internal/core/ports/repositories"
	"github.com/emoss08/trenova/internal/infrastructure/config"
	"github.com/emoss08/trenova/internal/infrastructure/database/common"
	"github.com/emoss08/trenova/internal/infrastructure/database/seeder"
	"github.com/emoss08/trenova/internal/infrastructure/database/seeds"
	"github.com/emoss08/trenova/internal/infrastructure/postgres"
	"github.com/emoss08/trenova/internal/infrastructure/postgres/repositories/fiscalperiodrepository"
	"github.com/emoss08/trenova/internal/infrastructure/postgres/repositories/fiscalyearrepository"
	"github.com/emoss08/trenova/internal/infrastructure/postgres/repositories/journalpostingrepository"
	"github.com/emoss08/trenova/internal/testutil/seedtest"
	"github.com/emoss08/trenova/pkg/pagination"
	"github.com/emoss08/trenova/shared/pulid"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"go.uber.org/zap"
)

type seededSourceOrg struct {
	ID             pulid.ID `bun:"id"`
	BusinessUnitID pulid.ID `bun:"business_unit_id"`
}
type seededSourceUser struct {
	ID pulid.ID `bun:"id"`
}
type seededSourceAccount struct {
	ID pulid.ID `bun:"id"`
}

func TestGetByObjectReturnsLatestJournalSource(t *testing.T) {
	ctx, db, cleanup := seedtest.SetupTestDB(t)
	defer cleanup()
	seedRegistry := seeder.NewRegistry()
	seeds.Register(seedRegistry)
	engine := seeder.NewEngine(db, seedRegistry, &config.Config{System: config.SystemConfig{SystemUserPassword: "test-system-password"}})
	_, err := engine.Execute(ctx, seeder.ExecuteOptions{Environment: common.EnvDevelopment})
	require.NoError(t, err)

	conn := postgres.NewTestConnection(db)
	repo := New(Params{DB: conn, Logger: zap.NewNop()})
	postingRepo := journalpostingrepository.New(journalpostingrepository.Params{DB: conn, Logger: zap.NewNop()})
	orgID, buID, userID, fiscalYearID, periodID, accountIDs := setupJournalSourceFixture(t, ctx, conn)
	now := time.Date(2027, time.January, 15, 0, 0, 0, 0, time.UTC).Unix()
	sourceObjectID := pulid.MustNew("inv_").String()

	require.NoError(t, postingRepo.CreatePosting(ctx, repositories.CreateJournalPostingParams{BatchID: pulid.MustNew("jb_"), OrganizationID: orgID, BusinessUnitID: buID, BatchNumber: "JB-SRC", BatchType: "System", BatchStatus: "Posted", BatchDescription: "Source seed", FiscalYearID: fiscalYearID, FiscalPeriodID: periodID, AccountingDate: now, PostedAt: &now, PostedByID: userID, CreatedByID: userID, UpdatedByID: userID, EntryID: pulid.MustNew("je_"), EntryNumber: "JE-SRC", EntryType: "Standard", EntryStatus: "Posted", ReferenceNumber: "INV-1", ReferenceType: "InvoicePosted", ReferenceID: sourceObjectID, EntryDescription: "Source seed", TotalDebit: 1000, TotalCredit: 1000, IsPosted: true, IsAutoGenerated: true, RequiresApproval: false, IsApproved: true, ApprovedByID: userID, ApprovedAt: &now, SourceID: pulid.MustNew("jsrc_"), SourceObjectType: "Invoice", SourceObjectID: sourceObjectID, SourceEventType: "InvoicePosted", SourceStatus: "Posted", SourceDocumentNumber: "INV-1", SourceIdempotencyKey: "invoice-posted:test-src", Lines: []repositories.JournalPostingLine{{ID: pulid.MustNew("jel_"), GLAccountID: accountIDs[0], LineNumber: 1, Description: "Debit", DebitAmount: 1000, NetAmount: 1000}, {ID: pulid.MustNew("jel_"), GLAccountID: accountIDs[1], LineNumber: 2, Description: "Credit", CreditAmount: 1000, NetAmount: -1000}}}))

	source, err := repo.GetByObject(ctx, repositories.GetJournalSourceByObjectRequest{TenantInfo: pagination.TenantInfo{OrgID: orgID, BuID: buID}, SourceObjectType: "Invoice", SourceObjectID: sourceObjectID})
	require.NoError(t, err)
	require.NotNil(t, source)
	assert.Equal(t, "Invoice", source.SourceObjectType)
	assert.Equal(t, sourceObjectID, source.SourceObjectID)
	assert.Equal(t, "InvoicePosted", source.SourceEventType)
	assert.Equal(t, "Posted", source.Status)
	assert.True(t, source.JournalEntryID.IsNotNil())
}

func setupJournalSourceFixture(t *testing.T, ctx context.Context, conn *postgres.Connection) (pulid.ID, pulid.ID, pulid.ID, pulid.ID, pulid.ID, []pulid.ID) {
	t.Helper()
	logger := zap.NewNop()
	fiscalYearRepo := fiscalyearrepository.New(fiscalyearrepository.Params{DB: conn, Logger: logger})
	fiscalPeriodRepo := fiscalperiodrepository.New(fiscalperiodrepository.Params{DB: conn, Logger: logger})
	var org seededSourceOrg
	require.NoError(t, conn.DB().NewSelect().Table("organizations").Column("id", "business_unit_id").Limit(1).Scan(ctx, &org))
	var user seededSourceUser
	require.NoError(t, conn.DB().NewSelect().Table("users").Column("id").Where("current_organization_id = ?", org.ID).Where("business_unit_id = ?", org.BusinessUnitID).Limit(1).Scan(ctx, &user))
	accounts := make([]seededSourceAccount, 0, 2)
	require.NoError(t, conn.DB().NewSelect().Table("gl_accounts").Column("id").Where("organization_id = ?", org.ID).Where("business_unit_id = ?", org.BusinessUnitID).Where("status = 'Active'").Order("account_code ASC").Limit(2).Scan(ctx, &accounts))
	require.Len(t, accounts, 2)
	fy, err := fiscalYearRepo.Create(ctx, &fiscalyear.FiscalYear{OrganizationID: org.ID, BusinessUnitID: org.BusinessUnitID, Status: fiscalyear.StatusOpen, Year: 2027, Name: "FY 2027", StartDate: time.Date(2027, time.January, 1, 0, 0, 0, 0, time.UTC).Unix(), EndDate: time.Date(2027, time.December, 31, 23, 59, 59, 0, time.UTC).Unix(), IsCurrent: true, AllowAdjustingEntries: true})
	require.NoError(t, err)
	period, err := fiscalPeriodRepo.Create(ctx, &fiscalperiod.FiscalPeriod{OrganizationID: org.ID, BusinessUnitID: org.BusinessUnitID, FiscalYearID: fy.ID, PeriodNumber: 1, PeriodType: fiscalperiod.PeriodTypeMonth, Status: fiscalperiod.StatusOpen, Name: "January 2027", StartDate: time.Date(2027, time.January, 1, 0, 0, 0, 0, time.UTC).Unix(), EndDate: time.Date(2027, time.January, 31, 23, 59, 59, 0, time.UTC).Unix(), AllowAdjustingEntries: true})
	require.NoError(t, err)
	return org.ID, org.BusinessUnitID, user.ID, fy.ID, period.ID, []pulid.ID{accounts[0].ID, accounts[1].ID}
}
